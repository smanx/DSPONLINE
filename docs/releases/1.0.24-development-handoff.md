# 1.0.24 工厂管理功能开发交接

```yaml
handoffSchema: dspidle-development-handoff/v1
role: develop
developmentStatus: complete
releaseTarget: 1.0.24
releaseReady: false
releaseAuthorization: pending
sourceCommit: 9f960291740a112b2dafc8f839f773d22fc379d1
sourceVersionAtCommit: 1.0.23
immutableArtifacts: not-generated
releaseManifest: not-generated
lastVerifiedDate: 2026-08-03
```

> 轮询规则：`developmentStatus: complete` 只表示本批源码和回归测试已完成，不是生产发布授权。发布 Agent 只有在用户明确授权发布、1.0.24 版本戳已由开发角色提交、不可变制品和清单已生成并验证后，才能把 `releaseReady` 视为满足。当前 `releaseReady: false`，禁止使用 dirty 工作区的 `dist/`、临时安装包或自行创建的签名证书发布。

## 交接摘要

- **Task ID / title**：`FACTORY-MGMT-20260803 / 工厂管理与大堆叠兼容六项开发`
- **Priority**：建筑堆叠读取兼容为 P0；物流管理、传送带选中、建筑回收、物品快捷操作、手机蓝图导入为 P1。
- **Source and attachments**：2026-08-03 用户直接开发需求；本批没有附件夹具，也没有使用或上传玩家存档。
- **Reproduction or observed evidence**：旧读取校验会把一亿以上历史堆叠按新增上限处理；物流缺少跨行星集中入口；连接线路不返回实际线路 ID；建筑托盘缺少回收入口；物品卡和手机蓝图库缺少所述快捷操作。对应修复已由下述单元与浏览器回归复核。
- **User-visible acceptance criteria**：六项验收状态见“验收标准状态”表；禁止静默截断历史安全整数，所有状态修改继续通过引擎纯函数/`commitGame` 边界。
- **Target platforms**：桌面 Web、移动 Web、Windows Electron、Android WebView；本次自动化实际覆盖桌面/触摸仿真 Chromium，实体平台缺口在风险章节列明。
- **Required tests**：TypeScript、完整 Vitest、服务端测试、生产构建、完整 Playwright，以及发布候选阶段的实体 Android/Windows 签名包 smoke test。
- **Release target and version**：目标版本 1.0.24；开发提交仍声明 1.0.23，正式候选尚未执行版本戳同步。
- **Compatibility boundary**：GameState v46、存档 envelope v2、云 schema v7、SQLite layout v2，均未升级。
- **Commit SHA**：`9f960291740a112b2dafc8f839f773d22fc379d1`（`feat: add factory management workflows`）。

## 已完成内容与证据

### 1. 建筑堆叠新增上限与历史存档保留（P0）

- `MAX_BUILDING_STACK_COUNT` 统一为 100,000,000；新增、矿机安装、堆叠调整、批量建造和蓝图部署等入口禁止继续增加到上限以上，并返回明确原因。
- 读取存档不再把历史 `machineCount`、`minerCount` 或历史蓝图中的安全整数静默截断到一亿。已有大于一亿且不超过 `Number.MAX_SAFE_INTEGER` 的值原样保留，可回收但不能继续增加。
- 0、负数、分数和不安全整数被拒绝，并给出备份/救援提示，避免精度损失或静默清空。
- 蓝图导入要求正安全整数且不超过统一新增上限，错误包含实际字段值和允许范围；边界值可完整导入、导出和再次解析。
- 云端 v46 校验接受历史安全超限的实体、蓝图和不可变蓝图版本，拒绝超过安全整数的危险值。

### 2. 独立物流管理页面（P1）

- 运营中心新增“物流管理”页，按恒星系、行星和物流塔分层展示，支持搜索、类型与异常筛选；只渲染展开的树节点。
- 支持行星物流站、星际物流站和轨道采集器，显示传统/量子状态，并编辑槽位、供需、本地/星际模式、优先级、库存上下限、舰队、翘曲器、自动补充和堆叠数量等现有能力。
- 远程编辑通过引擎域命令完成，不临时切换 `activePlanetId`。槽位改物品沿用危险操作确认边界。
- 筛选与展开状态只属于 UI，不写入 GameState。移动端控件达到 44 px，并验证 200% 字号横竖屏无横向溢出。

### 3. 连接传送带后精确选中线路（P1）

- `connectBeltWithResult()` 返回实际受影响的 `beltId`，新建线路和既有线路增加并联数均可精确定位。
- 匹配包含 source、target、itemId、targetPortIndex 和 tier；成功后清空建筑选择、设置单/多线路选择并打开 Inspector。
- 原 `connectBelt()` 保持兼容包装，不改变自动配方、端口规则或库存扣除。

### 4. 建筑托盘回收模式（P1）

- Construction Dock 增加回收入口，复用已有 selectionMode、单选、Shift 多选、框选、触摸选择和二次确认流程。
- 确认界面显示建筑数、相关传送带数和预计返还；提交仍调用 `removeEntities` 等域命令，未在组件中直接删除状态数组。
- 物流站、建筑制造中心等特殊建筑继续使用现有清理、任务取消和物资返还规则。

### 5. 物品悬浮卡快捷操作（P1）

- Item hover card 新增“定位”和“打开图鉴”，由 App 注入 `locateProductionLine` 与 `openRecipeFocus` 行为。
- 无生产来源时定位不可用并说明原因；Portal 支持从物品移动到卡片后继续交互。
- 移动端长按打开，两个操作区均至少 44 px。

### 6. 手机版蓝图导入（P1）

- 新版和经典手机 UI 均显示带文字的“导入蓝图”按钮，可触发现有 file input，并保留文件导入与粘贴 JSON 两种入口。
- 错误显示版本/格式等具体原因；成功显示建筑和传送带摘要并刷新编辑内容。
- 已验证 390x844 经典竖屏、844x390 新版横屏和 200% 字号。

## 验收标准状态

| 验收项 | 状态 | 本次证据 |
| --- | --- | --- |
| 新增堆叠不超过一亿 | 通过 | 引擎单测与浏览器完整交互 |
| 历史安全超限存档不丢失 | 通过 | 保存、加载、导出、重载与云校验测试 |
| 物流管理可搜索并安全远程编辑 | 通过 | 引擎域命令单测和桌面/移动 E2E |
| 连接后选中正确新建/并联线路 | 通过 | 精确 `beltId` 单测与 E2E |
| 回收模式支持单选、多选、确认和返还 | 通过 | 预览/删除单测与 E2E |
| 物品卡定位与图鉴操作可交互 | 通过 | Portal、桌面指针和触摸长按 E2E |
| 手机蓝图导入可见并可 `setInputFiles` | 通过 | 经典竖屏、新版横屏 E2E |
| 旧存档、旧蓝图和玩家库存兼容 | 通过（自动化范围内） | GameState/envelope/schema 未变；完整单元与 E2E 回归通过 |

## 修改文件

提交共修改 21 个文件，新增 1,748 行、删除 109 行：

```text
docs/GAMEPLAY_SYSTEMS.md
server/index.mjs
server/server.test.mjs
src/App.tsx
src/components/BlueprintWorkspace.tsx
src/components/GamePanels.tsx
src/components/ItemReference.tsx
src/components/LogisticsManagementPanel.tsx
src/components/OperationsWorkspace.tsx
src/components/mobile/MobileWorkspaceHub.tsx
src/game/blueprintExchange.test.ts
src/game/blueprintExchange.ts
src/game/engine.test.ts
src/game/engine.ts
src/game/storage.test.ts
src/game/storage.ts
src/i18n/legacyTranslations.ts
src/styles.css
src/styles/mobile-workspaces.css
tests/e2e/game-flow.spec.ts
tests/e2e/v124-factory-management.spec.ts
```

## 本次准确测试结果

以下结果来自检出精确提交 `9f960291` 的隔离 worktree，不包含当前主工作区中另一条 P6 多 Worker 未提交改动：

| 命令 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过，0 个 TypeScript 错误 |
| `npm test -- --run` | 74 个测试文件通过、1 个跳过；674 项通过、9 项跳过；0 失败 |
| `npm run test:server` | 46/46 通过；0 失败 |
| `npm run build` | 通过；Vite 转换 1,868 个模块；仅保留现有大于 500 kB chunk 警告 |
| `npx playwright test --workers=1` | 211 项通过、3 项跳过、0 失败 |
| `git diff --check` | 通过 |

新增 `v124-factory-management.spec.ts` 的 9 个桌面/移动场景均实际运行并通过：一亿新增上限与历史堆叠重载、物流远程编辑、精确线路选择、单/多选回收返还、物品卡 Portal 快捷操作、200% 字号移动物流、触摸长按、经典竖屏蓝图导入、新版横屏蓝图导入。

Playwright 的 3 个跳过项依赖未设置的 `DSP_CLOUD_UPLOAD_FIXTURE`，与本批六项功能无直接关系。运行期间本地 API `127.0.0.1:4320` 未启动，日志反复出现 `ECONNREFUSED`；离线 UI 路径仍通过，且服务端 46 项单测全部通过，但本次没有完成真实 API 代理 E2E。

## 制品、清单与发布门禁

- **Artifact paths**：未生成。隔离 worktree 中的 `npm run build` 只用于验证，不是不可变发布制品，不能发布其临时 `dist/`。
- **Manifest and aggregate hash**：未生成，不可用。
- **Web/API archives**：未生成。
- **Android APK / stable manifest**：未生成，也未读取长期 Android keystore。
- **Windows installer / latest.yml**：未生成，也未读取 Windows 签名证书。
- **Download-site archive**：未生成。
- **Version stamp**：提交中的 `package.json` 仍为 1.0.23；Android versionCode、Windows 元数据、公告和原生版本文件尚未同步到 1.0.24。
- **Production authorization**：未授予。本开发任务明确禁止 VPS、下载页和生产发布。

发布前必须由开发角色从 clean、可追溯提交完成 1.0.24 版本戳和原生版本同步，重新运行受影响门禁，并生成 Web/API/原生/下载页不可变制品及 release manifest。清单必须记录 Git SHA、build ID、文件数、逐文件 SHA-256、聚合 SHA-256、平台签名指纹和制品路径。任一项缺失时，发布 Agent 必须停止并报告阻塞，不得从当前 dirty 主工作区临时构建。

## 发布 Agent 后续检查清单

用户明确授权且制品门禁补齐后，Release 角色应按以下顺序执行：

1. 读取本交接、`docs/PROJECT_STATUS.md`、`docs/DEPLOYMENT_OPERATIONS.md`、`docs/TESTING_RELEASE.md` 和 Skill 的 release 角色清单；复核候选提交、版本、build ID、manifest 与全部哈希。
2. 验证 Android APK 与上一正式版使用相同长期签名且 versionCode 递增；验证 Windows 安装包签名、官方 HTTPS 云 API、更新地址和 stable 通道。证书缺失或指纹不一致时停止，禁止生成替代证书。
3. 将香港和上海视为独立节点，分别读取当前 Web/API `current`、上一回滚目录、服务、Nginx、磁盘余量和数据库 schema。禁止互相代理或共用数据库。
4. 后端有任何变化前，分别调用 Backup API 创建 SQLite 备份，验证文件大小、`quick_check` 和可读性；不触碰玩家存档正文。
5. 上传到两个节点各自的新 release 目录，在未激活目录验证依赖、manifest 和 API 启动；不得覆盖现有目录或在服务器热改源码。
6. 使用原子切换脚本逐节点切换，并验证本机/公网 `/api/health`、`version.json`、根页面、hashed assets、service worker、manifest、缓存头和更新提示。失败立即切回记录好的旧指针，数据库不随代码回滚。
7. 下载页使用新 release 目录，核对 APK、EXE、`stable.json`、`latest.yml`、`release.json` 的版本、大小、SHA-256、Range 与缓存头；只有全部匹配后才原子切换下载页 `current`。
8. 用专用测试数据执行本批六项最小 smoke test，不把玩家真实存档上传到生产账号。记录香港、上海和下载页各自的旧/新目录、健康结果与回滚指针。

## 风险与未验证项

- 没有在实体 Android WebView、实体手机浏览器、桌面 Edge 或 Windows Electron 安装包内运行本批流程；移动验证使用 Chromium 触摸仿真。
- 没有启动本地 API，因此没有浏览器到真实代理/SQLite 的端到端云上传测试。云端历史大堆叠兼容由 `server.test.mjs` 的临时数据库测试覆盖。
- 物流管理页采用展开节点渲染，但尚未用数千物流塔的真实玩家存档做长时间滚动、内存和交互性能基准。
- 200% 字号验证覆盖指定移动横竖屏；80/100/125/150% 和更多桌面分辨率由完整 E2E 的既有范围间接覆盖，未逐一做人工视觉截图审查。
- 当前主工作区含与本提交无关的 P6 多 Worker 未提交修改。它们不属于 1.0.24 本批，也没有进入隔离验证；发布时必须以 clean commit/manifest 为准。
- 生产节点当前版本、磁盘、备份状态和回滚目录未在开发会话中读取，不能从旧文档推断。Release 角色必须现场只读核验。

## 回滚边界

本批不迁移 GameState、存档 envelope、云 schema 或 SQLite layout，因此代码回滚不应要求数据库回滚。发布角色必须在每个节点切换前记录实际 `current` 和 `previous-release` 指针；出现 UI、读取、API 或安装包回归时原子切回现场记录的上一 Web/API/下载页目录，保留新版本日志和制品供诊断。除非经过独立灾难恢复授权，禁止恢复旧数据库或删除当前玩家数据。
