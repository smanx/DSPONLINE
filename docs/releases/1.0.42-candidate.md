# DSPidle2 1.0.42 候选记录（重建中）

> 状态：开发侧 No-Go；新的固定运行时 SHA、Build ID 和不可变制品尚未生成
> 分支：`codex/1.0.42-ui-review`
> 最终生产父级：`1.0.41+2e43f5644241`

旧 `8056d2cb0e1b46757676b490c08f28a1e66fd0a6 / 1.0.42+8056d2cb0e1b` 候选建立在 1.0.41 初始源码之上，未包含香港 P0 热修、35 MiB 本地存档恢复、增产剂上限、无限矿物速通和未提交时间扭曲预算恢复，现已作废。其不可变目录只作历史诊断保留，不得发布、覆盖或复用 Build ID。

当前开发范围和测试矩阵见 [1.0.42 开发总纲](../1.0.42_UI_REVIEW_DEVELOPMENT_PLAN.md)。完成以下事项后，本文件才可替换为正式候选记录：

1. 从最终代码和架构文档提交获得新的 clean runtime SHA 与唯一 Build ID。
2. 在该 SHA 上从头运行完整 Vitest、server、ops、native、Chromium、Firefox/WebKit、production-preview PWA、typecheck、build、licenses 和生产依赖审计。
3. 从固定 SHA 重建 Web/API/source、Android unsigned APK/AAB、Windows unpacked diagnostic。
4. 生成并复验 source manifest、conditional skips、SBOM、candidate manifest、SHA-256 和 provenance。
5. 记录所有条件跳过、真机/签名/Linux 发布侧剩余门禁及只回滚代码、不恢复数据库的方案。

开发过程不连接生产，不使用真实玩家账号或生产数据库做写入测试，不写入附件存档，不修改云历史或排行榜，不部署、不切流、不更新下载页、不覆盖 stable。
