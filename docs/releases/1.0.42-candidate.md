# DSPidle2 1.0.42 开发候选记录

> 候选完成日期：2026-08-14
> 固定运行时源码：`8056d2cb0e1b46757676b490c08f28a1e66fd0a6`
> Build ID：`1.0.42+8056d2cb0e1b`
> 开发分支：`codex/1.0.42-ui-review`
> 制品目录：`D:\GameDev\DSPidle2-v142-release\1.0.42-8056d2cb0e1b`
> 状态：开发侧完成、未部署；正式签名、真实设备和生产发布门禁完成前继续 **No-Go**

## 结论

1.0.42 的 UI 全面复核优化已进入固定运行时提交。工作区统一读取动态顶栏/底部托盘安全区；手机命令面板到工作区、抽屉和实体定位使用原子 history 替换；全屏工作区统一背景 inert、焦点圈定、嵌套 Portal 与关闭恢复；统计交互语义、亮暗主题文字对比度、窄屏/高字号重排和触控命中区已收口；中文组合输入及页面内草稿不再被父级刷新或响应式重挂载覆盖；教程显示真实应用版本，进度按独立内容 revision 保存。

本版不改玩法平衡、离线/纯挂机结算、生产、资源守恒、排行榜或云存档协议。开发过程没有连接生产服务器，没有读取或写入玩家存档，没有使用真实账号/生产数据库，没有修改排行榜历史，没有部署、切流、更新下载页或覆盖 stable 制品。

## 兼容矩阵

| 层 | 1.0.42 候选 | 结论 |
| --- | --- | --- |
| GameState / envelope | v46 / v2 | 不变，无新增必填字段 |
| 云 schema / SQLite layout | v7 / v2 | 不变，无数据库迁移 |
| 模式与槽位 | `normal/speedrun × main/1/2/3` | 继续隔离，本版不触碰键或转换规则 |
| 教程进度 | `2026-08-14-r1` 内容 revision | 只在当前键缺失时复制合法旧进度；不删除旧键、不覆盖新进度，重复执行幂等 |
| 文字草稿 | 页面内存 | 不进入 GameState、IndexedDB、localStorage、云端或日志；密码明确排除 |
| Web/PWA | 1.0.42 | `version.json` Build ID 与固定 SHA 一致 |
| Android | 1.0.42 / 1000042 | unsigned APK/AAB，仅诊断，不能进入 stable |
| Windows | 1.0.42 / 1.0.42.0 | unpacked、官方地址为空、Authenticode `NotSigned`，仅诊断 |
| API | 与 1.0.41 相同 | 162 文件展开包临时 SQLite health 200；schema/layout 仍为 7/2 |

1.0.41 旧存档不需要转换即可读取。教程迁移与普通存档迁移完全独立；损坏的旧教程进度会被忽略，但不会阻止游戏或修改存档。回滚到 1.0.41 时旧教程键仍保留，1.0.42 新键不会被 1.0.41 当作游戏存档处理。

## 修改范围

相对 1.0.41 交接基线 `93b984ebf7f765216f0a21f015bae03df5bc27b3`，固定运行时改动 89 个文件，约 `+1874/-372`。完整机器可复核清单：

```text
git diff --name-status 93b984ebf7f765216f0a21f015bae03df5bc27b3..8056d2cb0e1b46757676b490c08f28a1e66fd0a6
```

| 分组 | 主要文件 |
| --- | --- |
| 壳层与焦点 | `src/App.tsx`, `src/components/AccessibleDialog.tsx`, `src/components/WorkspaceFrame.tsx`, `src/components/GamePanels.tsx` |
| 工作区布局 | `src/components/*Workspace.tsx`, `src/styles.css`, `src/styles/codex.css`, `src/styles/mobile-workspaces.css`, `src/theme.css` |
| 手机导航 | `src/hooks/useMobileNavigation.ts`, `src/components/mobile/MobileGameShell.tsx`, `src/components/mobile/MobileWorkspaceHub.tsx` |
| 输入稳定 | `src/components/StableTextInput.tsx` 及蓝图、统计、账号、安全、搜索等调用方 |
| 教程与版本 | `src/game/tutorialProgress.ts`, `src/components/TutorialWorkspace.tsx`, `src/i18n/releaseNotes.ts`, `package*.json`, `android/native-version.properties` |
| 可访问性细节 | `src/components/StatisticsWorkspace.tsx`, `src/components/ItemReference.tsx`, `src/game/technologyTreeLayout.ts` |
| 回归 | `tests/e2e/v142-ui-review.spec.ts`, `tests/e2e/mobile-shell.spec.ts`, `tests/e2e/game-flow.spec.ts` 及相关单元/既有夹具 |

## 最终开发门禁

| 门禁 | 最终结果 |
| --- | --- |
| clean install | 根 456 包、server 75 包安装成功 |
| typecheck | 通过；候选各平台 build 也重新执行 TypeScript 检查 |
| Vitest | 139 文件通过/6 条件跳过；1222 通过/18 跳过；0 失败；117.64 秒 |
| server | 347 通过/2 条件跳过；0 失败；约 36.5 秒 |
| ops | 55 通过/6 个 Linux 专属跳过；0 失败；8.25 秒 |
| native | 24/24；0 失败 |
| Chromium | 344 通过/9 条件跳过；353 总项；0 失败；985.9 秒 |
| Firefox/WebKit | 2/2；0 失败；11.3 秒 |
| production preview PWA | 1/1；0 失败；7.0 秒 |
| production build | 通过；固定提交门禁首屏 184,589 B gzip，候选重建 184,570 B；菜单候选 273,830 B；禁止模块为空 |
| licenses | 125 个运行时包，清单一致 |
| production audit | 根项目 0；server 0 |
| API | 162 文件；aggregate `998c688a5128114c8060305ba815545ff7d2bad28eb424a764e1c50162b29083`；临时 SQLite health 200/schema 7/layout 2 |
| Android | bundle/assemble/lintVital/zipalign 通过；`cn.dsponline.network`、1.0.42/1000042；按预期 unsigned |
| Windows | unpacked、PE/ASAR/Build ID/48 MiB 传输契约通过；隔离 profile 10 秒 4 个候选进程；`NotSigned` |

首次完整 Chromium 诊断运行暴露了移动施工坐标和 Canvas 首帧时序两处真实回归；修复后目标用例及完整 353 项从头重跑为 0 失败。Android 第一次制品命令因隔离工作树没有机器专属 `local.properties` 而在 Gradle 产物阶段前停止；随后仅用现有 SDK 的临时环境变量重跑，未写机器路径到仓库，最终 bundle/APK/lint 全部通过。两次中间结果均未进入候选清单。

Chromium 的 9 条跳过来自 2 条 construction 真实存档、2 条 P6 真实终局/基准、1 条真实 Canvas、2 条真实离线/时间扭曲 describe、1 条真实 30 天纯挂机和 1 条开发服务器模式 PWA。条件报告包含 10 个声明匹配点（其中一个为报告脚本声明），不等同于 10 个实际跳过。相关真实夹具环境变量为空，本轮未读取玩家存档；PWA 已在生产预览单独通过。

完整开发依赖图仍报告 1 moderate/4 high 的构建工具链提示；根与 server 的 production audit 均为 0。不得在固定候选上直接运行自动修复造成 lockfile 漂移。

## 新增与强化的回归

- 10 项 `v142-ui-review` 浏览器矩阵：动态安全区、模态 inert/Portal/焦点恢复、统计语义、4.5:1 对比度、Axe、200% 手机布局、几何不重叠、中文组合输入、窄屏发布说明、粗指针和施工节点。
- 手机命令面板原子切换到工作区、物资抽屉、检查器和实体焦点，浏览器返回不二次撤销。
- `WorkspaceFrame` 3 项：背景失活与 Escape/焦点恢复、壳层导航保留、嵌套确认 Portal。
- `StableTextInput/Area` 8 项：中文组合、父级刷新、重挂载、resize/orientation/fullscreen/blur、草稿隔离、密码不共享、失焦单次提交。
- 教程进度 3 项：内容 revision 与应用版本独立、旧键一次迁移、损坏旧值安全忽略。
- 物品字形前景色 3 项、科技树高字号上限 1 项、1.0.42 发布说明稳定键与窄屏操作回归。

## 不可变制品

| 制品 | 字节 | SHA-256 |
| --- | ---: | --- |
| source tar | 6,114,303 | `787c95469ecf11c5152ecb067c2da67e3f993b2a99e20c641288e3fb58b0b7a4` |
| Web tar | 1,455,767 | `4a02181719a2b494442a6709c608d7f43a5d6036c0cbcbc59c5b39192f18ef18` |
| API expanded tar | 616,305 | `bf647d94523bbc62a49e09f97dfc418697f58885c3de93f2b424dd1f7da18c98` |
| Windows unpacked diagnostic unsigned | 154,655,950 | `16d198708fa7100800c0048a7ac702ab9bdc96f1d10be078f2e61aac964c6877` |
| Android unsigned APK | 4,830,240 | `f67bb5c51de691f71c66ca4ef183b5cf595ac52a1c5511379018c517f2535d59` |
| Android unsigned AAB | 4,644,965 | `15e8d41d8160756eafcdbd76d3f8e8caf3d6622489db9c944fb58a5f07153868` |
| source manifest | 36,061 | `0b02e9e842e1bdeed88762193b7bed9a924f65b7845f4fb1187268110b573394` |
| conditional skips | 3,438 | `6f95f649807e4b8ac23387f6adcb56983a35731797f69e10f8849d5835d1aa0b` |
| SBOM | 407,391 | `8dafd0e3343f6e128a86c7c33dc7279d2339009304b2044bb5debf6adb105727` |
| source verification | 176 | `74662eb523c7d82ded2eadeef8ae5dfc37e4947b9fa2f321b4cea836d98357bf` |
| candidate manifest | 1,997 | `507e97c943c656618ebc7545faa74b60b365c1d960704fe15511d36dc8baf0f6` |
| provenance | 2,385 | `a52897b1cb4dcfcfd807b26340373672e1f4eaf2347d08e2f2156bdb92f4745d` |

source manifest 为 213/213，聚合 SHA-256 为 `292a5ec74bb446f8ed71d157697c24723a9e04f5c2427666c61c1d7423281660`。candidate manifest 的 10 个 subject 已逐项复算大小与哈希；source/Web/API/Windows 四个 tar.gz 分别完整读取 968/122/166/8398 个目录项；provenance 的 3 个 subject 已复验。`version.json.generatedAt` 随构建变化，因此 provenance 明确 `reproducible:false`。

## 性能影响

- 候选首屏 184,570 B gzip，相对 1.0.41 的 183,885 B 增加 685 B（约 0.37%），仍低于 200 KiB 预算；菜单 273,830 B，相对增加 3,436 B（约 1.27%）。
- 动态安全区只在壳层尺寸变化时测量；焦点根只在工作区/嵌套模态生命周期查询，不进入模拟 tick。
- 稳定文字草稿只保存当前页面生命周期内的活动字符串，不持久化，不增加存档或云上传体积。
- 本版不改变 Worker、离线、纯挂机、生产、传送带、电力或物流算法，因此没有用降低模拟精度换取 UI 性能。

## 剩余风险与 No-Go

- Android/iOS 真机、微信/QQ 内置浏览器、鸿蒙浏览器、系统中文输入法、物理返回键、屏幕阅读器与真实 80%～200% 系统缩放仍需人工验收。
- 低配 Windows、Android 1.0.41→1.0.42 覆盖升级、Windows 覆盖升级、正式签名和证书连续性尚未执行。
- 1.0.41 继承的真实 Linux systemd/Nginx、UID/GID、flock、切换故障矩阵仍是生产硬门禁；本版未连接生产，不能用本地测试替代。
- 9 条真实夹具条件测试未执行，不能据此宣称所有玩家终局画布或真实大存档设备体验已验收。
- Windows/Android 当前制品为诊断件，缺少正式服务地址与签名；任何一个进入 stable 都是 No-Go。

## 回滚

- 未部署：废弃 `1.0.42-8056d2cb0e1b` 候选即可，1.0.41 发布工作树与现有生产不变。
- 已灰度：把 Web/API 静态和代码指针切回经过验证的上一不可变版本；只回滚代码，不恢复生产数据库，不删除云 revision，不修改排行榜历史或玩家存档。
- PWA：切回完整上一版 manifest、HTML、资源和 service worker，验证旧/新 cache 隔离；不得只覆盖入口 HTML。
- Android/Windows：正式包失败时保持上一 stable feed，不卸载玩家应用，不用新证书替代既有签名链。
- 教程迁移只复制旧进度且不删除旧键；代码回滚无需数据回滚。页面草稿从未持久化，无回滚数据。
