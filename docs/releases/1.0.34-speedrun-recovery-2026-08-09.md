# DSPidle2 香港历史速通成绩恢复记录（2026-08-09）

执行日期：2026-08-09

目标节点：香港正式节点

运行基线：`api-1.0.34-4a7d51241424` / schema v7 / SQLite layout v2

## 处置结论

在用户明确提供目标显示名、百万白糖显示时间 `13:42` 并授权备份及人工记录后，香港节点完成一次受控的历史速通成绩恢复。公开记录不保存玩家显示名、账号 ID、工厂 ID、存档正文或存档哈希。

只读锁定结果为：目标显示名精确匹配唯一账号；最近 20 条主云历史中仅修订 380 同时满足 GameState v46、`speedrun-v1`、`season_01`、合格速通身份、无内容包和百万白糖净增量 1,004,162。权威里程碑为 `822.75` 秒，客户端按 `Math.floor` 显示 `13:42`；历史档有效计时为 823 秒。当前主云档为修订 393，不再是速通工厂，因此现有“只接受最新主云档”的标准恢复工具按设计拒绝。

本次没有恢复、替换或修改玩家主云档，也没有把历史档切成当前档。事务只增加一条 `white_matrix_1m` verified submission 和一条最小化内部审计动作；原有另外两项速通成绩继续保留。

## 一次性工具门禁

标准 `server/speedrun-recovery.mjs` 的最新修订限制没有修改。此次使用不含目标明文的一次性离线工具，SHA-256 为：

`28a3de8b14390f942a0e6806cb2845aacb220c2af67bb2ae6cc1546164abebf8`

工具同时锁定：显示名 SHA-256、唯一账号、修订 380、完整正文 SHA-256、工厂身份、百万糖净增量、`822.75` 秒里程碑、823 秒有效计时、赛季、规则版本、v46、无内容包、当前无百万糖成绩、现有两条其他速通成绩、服务已停止和精确二次确认。

合成 SQLite 演练覆盖并通过：

- 无 `--service-stopped` 时拒绝。
- 错误确认文字时拒绝。
- 备份 `app_state` 或时间戳不一致时拒绝。
- 独立玩家存档证据 `quick_check=ok` 且正文 SHA-256 一致。
- 应用后云正文行数、历史和候选 payload 不变，只增加目标 submission 与审计。
- 第二次运行返回 idempotent，不增加重复成绩。

最终生产执行前，又在完整生产备份派生的 guard 副本上重复通过相同事务和幂等检查。一次性工具在验收后已从服务器临时目录删除，没有写入活动 API 发布目录。

## 备份

香港云服务、健康重启 timer 和异地备份 timer 全部停止后，使用 SQLite Backup API 创建完整一致性快照；备份脚本退出前完成 `quick_check`。所有文件均为 owner/group `ubuntu:ubuntu`、mode `0600`。

| 备份 | 字节 | SHA-256 | 用途 |
| --- | ---: | --- | --- |
| `/var/lib/dsp-idle-cloud/backups/pre-speedrun-recovery-20260809T053652Z.sqlite` | 2,667,495,424 | `1659fcad141370164e462e55de17eb8df8bd216257a4a3db0c226240b1011202` | 完整生产恢复点，`quick_check=ok` |
| `/var/lib/dsp-idle-cloud/backups/speedrun-recovery-guard-20260809T053652Z.sqlite` | 14,020,608 | `7653b499950f06a465039fa66d8e15af359b62f11f0532399de646c619c94a5a` | 精确匹配停服 `app_state` 与目标正文的事务 guard |
| `/var/lib/dsp-idle-cloud/backups/player-speedrun-evidence-20260809T053652Z.sqlite` | 270,336 | `ae3ea44d5e2b64424a35caf3acda64405f2a29d48b43fa0409a7f0ce051ab8ed` | 目标修订 380 的独立受保护存档证据，`quick_check=ok` |

完整生产备份和 guard 都在写入前生成。独立玩家证据库保存精确历史正文及内部映射，但其内容不得输出、提交或公开分发。

## 生产事务结果

生产服务保持停止时执行乐观锁事务；写前和写后均完成全库 `quick_check`。

| 不变量 | 写前 | 写后 |
| --- | ---: | ---: |
| 用户 | 686 | 686 |
| 主云档 | 558 | 558 |
| 目标账号主云历史 | 20 | 20 |
| 速通 submissions | 2 | 3 |
| 审计日志 | 1,728 | 1,729 |

事务内部还逐行确认：`cloud_save_payloads` 总行数和目标账号正文行数不变，修订 380 payload 与备份逐字一致，当前修订 393 和目标 20 条历史没有修改；去掉新增 submission 和新增审计后，事务内 `app_state` 与备份逐字一致。

新增成绩使用历史档权威里程碑 `822.75` 秒，没有人为改写成更快的整数 822 秒。客户端展示函数向下取整后为 `13:42`。

## 重启与公开验收

服务恢复后：

- `dsp-idle-cloud.service` active，`NRestarts=0`。
- `dsp-idle-healthcheck.timer` 与 `dsp-idle-offsite-backup.timer` active。
- journal warning 数为 0。
- 本机和公网 `/api/health` 均为 200，schema v7 / SQLite layout v2。
- 百万白糖公开榜存在唯一目标记录，`verified=true`、权威值 `822.75`、客户端显示 `13:42`。
- 同一账号原有另外两项速通目标仍各有一条 verified 记录。
- 当前主云修订仍为 393，目标历史仍为 20 条，修订 380 正文与完整备份、guard 和独立证据库一致。
- 香港 Web/API 指针仍为 1.0.34；1.0.35 Web-only canary、上海节点和稳定下载源均未改变。

恢复在线流量后，系统正常新增了一个匿名玩家记录和另一个账号的一次云上传，因此全局玩家/历史/payload 行数在重启后继续增长；没有账号、会话、云档、普通榜或历史记录被删除。事务完成瞬间的“不变”证据以上节停服后验为准。

收口时香港根盘使用率为 76%，可用约 10.1 GB，低于 80% 告警阈值。完整备份、guard 和独立玩家证据均保留，未删除任何已有有效备份。

## 回滚边界

不要通过恢复整库撤销这条成绩：服务恢复后已经存在其他玩家的正常写入，恢复旧数据库会覆盖这些新数据。

如未来经新的审计批准撤销，应先再创建当前数据库一致性备份，然后停服运行新的精确事务，只删除本次 submission，并追加对应撤销审计；不得修改云存档正文、当前修订或其他玩家记录。上表完整备份只作为灾难恢复点，不是日常榜单撤销命令。
