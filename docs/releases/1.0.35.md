# DSPidle2 1.0.35 正式 stable 发布记录

发布日期：2026-08-09

Release ID：`1.0.35-080844f55852`

Build ID：`1.0.35+080844f55852`

权威源码：`080844f55852995341a5f251d8f91bf090b9403f`

## 发布结论

`1.0.35` 已完成香港、上海 Web/API、上海下载页、Android stable APK 和 Windows stable 测试包正式发布。两地 Web/API 当前均为 `web-1.0.35-080844f55852` / `api-1.0.35-080844f55852`；上海下载站为 `download-site-1.0.35-080844f55852`。GameState v46、存档 envelope v2、云 schema v7 和 SQLite layout v2 均未变化。

用户明确豁免且只豁免精确候选 `1.0.35-080844f55852` 的正式 stable 物理真机门禁，授权继续发布香港、上海和下载页。该豁免不等于物理 Android、低配 Windows、锁屏、后台、温度、耗电或长时真实存档已经通过；其他源码、测试、签名、备份、隔离启动、原子切换和公网门禁没有放宽。

此前香港 Web-only Canary `1.0.35+48c74b7100dc` 的 Nginx 路由已通过发布前记录的配置备份移除，正式根 Service Worker 不再包含 Canary 特例。Canary 静态目录保留作审计，但不再有公开版本化路由，也没有进入 stable feed。

## 最终本地门禁

| 检查 | 结果 |
| --- | --- |
| `npm ci` | 根项目与 server 均通过 |
| TypeScript | 通过 |
| Vitest | 98 文件通过、5 文件跳过；887 项通过、16 项条件夹具跳过、0 失败 |
| Server | 70 项通过、2 项可选夹具跳过、0 失败 |
| Ops / native tools | 6/6、8/8 |
| Playwright | 266 总数；255 通过、11 条显式真实夹具跳过、0 失败 |
| 依赖 | 128 个运行时包许可证一致；根与 server 生产依赖审计均为 0 漏洞 |
| Build | 1,887 modules |

最终 Playwright 只针对 `080844f55852` 的 clean 候选完整重跑。Android 另完成 API 36.1 模拟器上的 `1.0.34 / 1000034` → `1.0.35 / 1000035` 同签名覆盖升级，`firstInstallTime` 不变、进程存活且 Fatal/ANR 为 0；没有把模拟器结果描述为物理真机结果。

## 不可变制品

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| Source manifest | 160 files | `160a8e1dc8e06ebf8eee8db0346a9b8567ee690a9b9d106665ba182634a79ecd` |
| Source aggregate | 160 files | `1179f0cfd561628d7779ab407a7bf0c65dba3b7094133fe5fc9d9d65821f5fe7` |
| Web archive | 1,341,181 | `e6925aa894d31aad4274893681d9dbd64daf7fdc98b5d7170c871e754eb07c4a` |
| API archive | 106,452 | `b41d1febef2f70ea1050d1846ad371bc87efe0375d79bc830f9ba7ded5ecac55` |
| Android APK | 4,861,729 | `56598fecf674c05141535a4fa99b868c16b4c6ccc6acdf7358a6f305a3c8e88a` |
| Android AAB（归档，不进入下载页） | 4,650,628 | `0e0c042ec370424a8860d92ec7463022623cee5a0a8e9b5a6ce96f432ab0572c` |
| Windows setup | 112,440,871 | `ea4ceb1625b69347a0207dac86d10cb0314b63738f84d5d3379481d55a67d322` |
| Windows blockmap | 118,367 | `1641d9c7bdf0901f2b0923715e5a50fac931a0adfc0de29283c2f670fc4d2cdf` |
| `latest.yml` | 356 | `a1e4459ec5a63c972ec46d5e58e9957879c0fca8155333253df931a38acc3221` |
| Download-site archive | 116,143,824 | `8b5496699b01506226789338d1a17d4ccf69fc5be5f1e301369ae5a82362b195` |

Source manifest 为 160/160；下载 manifest 为 9/9，聚合 SHA-256 `bb0e08bb92a3ee825b5bf2a4dc6265396a7b76edd5b3690acf3fed6c51f665d1`；bundle manifest 为 10/10，聚合 SHA-256 `354f4b2c6192703cea8cd44c48528bdcdcb12c0af4a7af06729bce3524a072b5`。Web/API 独立解包后按 source manifest 逐文件复验，API 禁止内容扫描为 0，不含数据库、备份、玩家正文、环境文件、PEM、P12、keystore、密码或 token。

Android 为 `1.0.35 / 1000035`，APK Signature Scheme v2/v3 有效，批准证书 SHA-256 为 `ede2aa09ed143a3fbeb283aad0e7801d192c851f240cd39278c399918a0216ce`。Windows FileVersion/ProductVersion 为 1.0.35，公开安装程序和解包 EXE 的 Authenticode 均为 `NotSigned`；没有创建或替换证书，下载页明确提示“未知发布者”与 SmartScreen 风险。

## 发布前备份

两地数据库保持独立，没有跨节点复制、合并、恢复、初始化或替换。香港大库在停止健康探针与 API 写入的短维护窗口中通过 SQLite Backup API 直接写入受保护归档；主体完成后先恢复 1.0.34 服务，再在备份副本上完成逐页校验和整文件哈希。上海小库使用本机受保护备份目录。两份均为 `0600`、`quick_check=ok`、schema v7/layout v2。

| 节点 | 大小 | SHA-256 | 脱敏记录摘要 |
| --- | ---: | --- | --- |
| 香港 | 2,776,186,880 | `a4a28b19ef0a2231018b3fa3003f6c60e4c3d7831db912319755bf162d3a8e14` | users 694、cloud saves 566、revisions 6,883、submissions 559、players 7,493、payload rows 7,094 |
| 上海 | 200,704 | `fa125d38c482415ecbabafc45c8195dc71a99937d2a28f178ec1d2d43905b169` | users/saves/revisions/payloads 0、players 42、errors 23 |

香港备份包含发布前已经按单独授权恢复的历史速通成绩；本轮发布没有运行历史成绩恢复、排行榜写入、云修订裁剪或真实账号写测试。代码回滚不恢复数据库。

## 未激活目录验证

Web/API 归档在两台 VPS 重新计算原始 SHA-256 后才解包到新目录；Web 为 127 个文件、API 为 33 个归档文件，上海下载站为 9 个文件。两地 API 运行 `npm ci --omit=dev` 并各自通过 70/72 服务测试，2 项仅因真实夹具不存在而跳过。

API 归档保留仓库 `server/` 前缀，而生产 systemd 固定从发布目录根部运行 `index.mjs`。Release Agent 在未激活目录把 `server/` 逐字展开到根部，保留原归档目录；所有源码逐文件 `cmp` 一致，并从生产入口根部再次通过两地完整服务测试。该步骤只改变运行目录布局，不改变源码或制品字节。

两地分别用自己的发布前备份派生临时数据库副本，在 `127.0.0.1:14320` 启动新 API，health 为 schema v7/layout v2，关键记录不减少。隔离进程停止后只删除临时数据库、WAL/SHM 和日志；受保护备份、生产库与发布目录保留。香港 2.8 GB 临时副本删除后根盘回到 76%。

## 原子切换与观察

发布顺序为香港 Web/API → 上海 Web/API → 上海下载页。Web/API 使用固定 `/usr/local/sbin/dsp-idle-switch-release`，切换前暂停健康、节点与异地备份 timer，使用扩展 health 窗口，成功后恢复 timer。上海下载页使用受保护状态文件和临时 symlink 原子切换。

香港新服务启动后按既有配置立即向 COS `auto` 前缀创建一次约 2.793 GB 快照，期间本机 health 曾因 I/O 超时；健康 timer 已暂停，服务没有崩溃或重启。快照收敛为 `ready` 后，本机连续健康通过并恢复三个 timer。发布收口时香港/上海 uptime 分别超过 24/20 分钟，服务 active、`NRestarts=0`，近 30 分钟 journal 错误命中为 0，各 DSP 专用访问日志最近 500 条 5xx 均为 0。根盘约为香港 76%、上海 83%。

旧 Canary 路由只比发布前 Nginx 备份多候选 worker 拒绝和版本化 alias。正式发布前已恢复原配置 SHA-256 `a28cd20218b883cf286322bda1980bd72b3d11a99a8774cafaaa718f87bda641`，另保留 pre-stable 安全副本；两次 `nginx -t` 和 reload 均通过。

## 公网验收

| 检查 | 结果 |
| --- | --- |
| 香港入口 | `https://dsponline.cn` 200，Build ID `1.0.35+080844f55852` |
| 上海入口 | 既有脱敏 HTTP 节点 `http://shanghai-node.example.invalid` 200，同一 Build ID；HTTP 页面不暴露密码输入 |
| 两地 API | 200，schema v7、SQLite layout v2、活动配置有效 |
| Web 资源 | 当前 1.0.35 与回滚版 1.0.34 hashed asset 均为 200、gzip、immutable；HTML/version/Service Worker no-cache |
| Origin | 两地各自正式 Origin 通过 |
| 下载域名 | `https://download.dsponline.cn` 证书有效至 2026-10-22，页面与 feeds 为 1.0.35 |
| 完整公网 APK | 4,861,729 bytes，SHA-256 与清单一致 |
| 完整公网 EXE | 112,440,871 bytes，SHA-256 与清单一致，Authenticode `NotSigned` |
| Range / cache | APK、EXE 均返回 `206`、1024 字节、正确 Content-Range；二进制 immutable，页面与 feeds no-cache |
| 香港下载入口 | APK 请求 302 到 `https://download.dsponline.cn` |
| Chrome smoke | 香港桌面/手机、上海桌面、下载页桌面/手机 5/5；0 页面错误、0 请求失败、0 破图、0 横向溢出 |
| PWA | 香港正式 worker 为唯一 active worker，无 waiting/installing 或刷新循环 |

公网 Chrome 使用临时 SSH 转发适配保持开启的 VPN/TUN；未添加系统路由、未关闭 TLS 或 host-key 校验。浏览器拦截了所有会写生产 API 的方法，没有登录账号、读取云槽、上传存档或写排行榜。

## 当前指针与回滚

- 香港 Web/API：`web-1.0.35-080844f55852` / `api-1.0.35-080844f55852`
- 香港直接代码回滚：`web-1.0.34-4a7d51241424` / `api-1.0.34-4a7d51241424`
- 上海 Web/API：`web-1.0.35-080844f55852` / `api-1.0.35-080844f55852`
- 上海直接代码回滚：`web-1.0.34-4a7d51241424` / `api-1.0.34-4a7d51241424`
- 上海下载页：`download-site-1.0.35-080844f55852`
- 上海下载页直接回滚：`download-site-1.0.34-4a7d51241424`

Web/API 回滚命令仍为 `sudo /usr/local/sbin/dsp-idle-switch-release --rollback-last`。下载页回滚只原子恢复下载 `current` 与状态文件。任何普通代码/下载回滚都继续使用当前生产数据库；不得恢复本次发布前备份，除非另行进入灾难恢复流程并取得明确授权。

## 明确未验证项

- 用户已豁免本候选的物理真机 stable 门禁；两台不同档位 Android、一台低配 Windows、锁屏/后台、温度/耗电和长时内存没有执行。
- 没有用真实生产账号完成四槽云写入，也没有上传真实玩家存档或测试夹具。
- Windows 仍是公开稳定测试包而非可信代码签名包；只能描述为 `NotSigned`。
- 香港每次服务启动仍会按既有无时间窗配置立即创建大型 COS 快照；本次已收敛，但后续重启仍应暂停健康 timer、预留 I/O 窗口，并评估显式低流量 `DSP_CLOUD_BACKUP_WINDOW`。
