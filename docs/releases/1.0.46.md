# DSP极简网络 1.0.46 正式发布记录

> 状态：**已正式发布并完成公网验收**  
> 发布日期：2026-08-20  
> Runtime Git SHA：`d5b60dc3b7d6046c14f6e46c2317f5a5e70a6c6c`  
> Release ID：`1.0.46-d5b60dc3b7d6`  
> Build ID：`1.0.46+d5b60dc3b7d6`

## 紧急修复

- 修复 Service Worker 从打包入口发现 `assets/...` 引用时错误解析成 `/assets/assets/...` 的问题。
- 新增根路径与 canary 路径的严格静态服务器测试；线上 Chrome 验收确认 worker active、147 个 shell 资源、0 个重复 assets 请求、离线根页面可用、页面错误为 0。

## 正式目标

- 香港：Web/API current 均为 `1.0.46-d5b60dc3b7d6`，release-control generation 25，green / 4322；health/ready/public health 均为 200，服务与 health timer active，`NRestarts=0`，pending 为空。
- 上海：Web/API current 均为 `1.0.46-d5b60dc3b7d6`，release-control generation 12，blue / 4321；health/ready 均为 200，服务 active，`NRestarts=0`，pending 为空。
- 上海下载页：current 为 `download-site-1.0.46-d5b60dc3b7d6`；下载页版本、Windows/Android feed、完整下载哈希、Range 206、不可变缓存均通过。
- 香港 `canary/previous` 继续指向独立的 `1.0.43-fceca3eda51c` 备份站，未被本次发布改变。

## 数据安全证据

- 香港切换前 Backup API 快照与独立 preflight 副本均为 3,772,833,792 bytes，SHA-256 `a841693bdc705b8d8665374c07d2456218049bb2be7c959633c1389ed9f35297`，`quick_check=ok`，schema v8 / layout v3；两份证据的大小和 SHA 完全一致。
- 香港切换后根盘 75%，低于 90% 保护线；数据库、WAL/SHM、既有备份及回滚目录均未删除或回滚。
- 上海切换前快照为 462,848 bytes，SHA-256 `31db6bed89bd8cba2f20a8caa9cd35a1c805c2e53fc1620946255bd5ded5c84f`，`quick_check=ok`，schema v8 / layout v3。

## 正式制品

- Web：155 文件；aggregate SHA-256 `914a3ddfb9a925997333200edb3246db1ab93dd37b7da86b1355cb5952000dad`。
- API：166 文件；aggregate SHA-256 `ea53c0ae5ee6be93db2d3b388fda968ef93d5ddbd5d77b8fa80dfd57dac88cd7`。
- Windows Setup：109,791,851 bytes，SHA-256 `35c78f6cb718bdaba97d6ea4102b31f85aa55e76f1510a3c3bef2e70eede9346`；按既有公开策略标记 `NotSigned`。
- Android APK：5,193,751 bytes，SHA-256 `0b39fc44ab3e384b50d1187ae8fffc3d083273d163b64daf95067f684a2906ad`；package `cn.dsponline.network`，versionCode `1000046`，v2/v3 与 zipalign 通过，证书连续性通过。
- Android AAB：4,982,601 bytes，SHA-256 `3fb906f2c74975abbf66d0e638c534b709926ccc1177298120d293c2d9b3d888`；证书连续性通过。`jarsigner -verify` 通过；strict 模式仅保留与历史获批 AAB 相同的自签名链警告。

## 门禁与发布后补测

- 发布前：PWA 专项 16/16、Web build/startup budget、生产预览 Playwright、Android 签名/包元数据、远端 manifest、备份/preflight、原子切换、公网 PWA、下载完整哈希及 Range 全部通过。
- 用户明确豁免 Android 实机门禁，并授权先发布、后补跑可延后的测试。
- 发布后：Vitest 173 文件 / 1436 tests 通过（7 文件 / 20 tests 按配置跳过）；Server 366 tests 通过（2 跳过）；Ops 56 tests 通过（6 个 Linux-only 环境项跳过）；Native 24/24 通过。
- 发布后完整 Playwright：426 passed / 26 skipped / 0 failed（6.4 分钟）。测试环境中预期的隔离 API 连接拒绝日志不影响断言结果。

## 回滚边界

- 玩家可用 Web 备份入口保持为香港 `canary/previous` → 1.0.43。
- release-control 的直接代码回滚边界为 1.0.45 API；两地生产库已是 schema v8 / layout v3，代码回滚不得恢复或降级数据库，也不得将旧的不兼容 API 直接接到生产库。
- 上海下载页 previous 保持 `download-site-1.0.44-3e580c715a5a`。
