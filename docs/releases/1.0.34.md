# DSPidle2 1.0.34 发布记录

发布日期：2026-08-08

Release ID：`1.0.34-4a7d51241424`

Build ID：`1.0.34+4a7d51241424`
权威源码：`4a7d51241424f4289c629377e896ec70f41cbe54`

## 发布结论

`1.0.34` 已完成香港、上海 Web/API、上海下载页、Android 稳定 APK 和 Windows 稳定测试包发布。两地 Web/API 当前均为 `web-1.0.34-4a7d51241424` / `api-1.0.34-4a7d51241424`；上海下载站当前为 `download-site-1.0.34-4a7d51241424`。GameState v46、存档 envelope v2、云 schema v7 和 SQLite layout v2 均未变化。

本版兼容历史唯一巨构安全堆叠，修复 Android 云上传、Web gzip 编码拒绝回退、纯挂机停止/保存恢复、排行榜实际结算吞吐与全星区理论吞吐口径，并补齐拉线起点及候选建筑高亮。旧 `9f9714f973b0` 及 unsigned/partial 诊断制品全部作废，没有发布。

Windows 按既有策略继续明确标记 `NotSigned`，没有创建或替换代码签名证书。Android 使用批准的长期证书，未创建新 keystore，未向 VPS 上传任何应用签名私钥。

## 最终源码与门禁

所有发布归档和原生安装包均从 clean source commit `4a7d51241424f4289c629377e896ec70f41cbe54` 构建；后续文档提交不进入二进制。

| 门禁 | 结果 |
| --- | --- |
| `npm ci` / 许可证 | 根项目与服务端安装通过；128 个运行时包许可证清单一致 |
| 类型检查 | `npm run typecheck` 通过 |
| Vitest | 92 个文件通过、5 个文件跳过；842 项通过、16 项显式跳过、0 失败 |
| 服务端 | 使用只读 19 MiB 吞吐夹具时 53 项通过、1 项可选测试跳过；夹具 SHA-256 未改变 |
| 运维 / 原生工具 | 6/6、8/8 |
| 生产构建 | 1,871 个模块；`dist/version.json` 为 `1.0.34+4a7d51241424` |
| Playwright | 265 项中 254 项通过、11 项显式可选夹具跳过、0 失败 |
| Git / manifest | 权威源码工作树 clean；source manifest 150/150 逐文件通过 |

服务端生产依赖审计为 0 漏洞。根项目当前注册表审计报告 1 个 high：打包工具链经 `electron-updater` / `electron-builder` 引入的 `js-yaml@4.3.0` `!!omap` 二次 CPU DoS 告警。它不在云服务生产依赖中，桌面更新源固定为受控 HTTPS 清单；本轮没有修改 lockfile 或重新签发已验证制品，风险保留跟踪。

## 不可变制品

| 制品 | 字节 | SHA-256 |
| --- | ---: | --- |
| Web `web-1.0.34-4a7d51241424-clean.tar.gz` | 1,321,163 | `3b496395bffffc69885212bdacdf04f64350aee7a0b712b801cc4932ba5cb7a6` |
| API `api-1.0.34-4a7d51241424-clean.tar.gz` | 84,461 | `faf55d48265bdaeba71ccb503440346d4f74af64e1bfcf2c7dcbbc51bfc5f7f5` |
| Android APK | 4,841,083 | `d556e6f3690cbe709d0f493019b55fdadc20658ef865bef9cbc71b1b1511a49e` |
| Windows x64 installer | 103,255,059 | `97c46f2de17539cc79886f10eeba0b3a9c94083671624b88f22970ab500f112f` |
| Windows blockmap | 110,926 | `40a5d812827a4343a816e766697277aaace9cb3d4ca5a461307f670d29eacaff` |
| 下载站归档 | 107,959,104 | `23ca6797d6721dd168f7135f3e1f976641f2b83106e58335d20d969e7903b492` |

| Manifest | 文件数 | 聚合 SHA-256 |
| --- | ---: | --- |
| `1.0.34-4a7d51241424.json` | 150 | `fe8844da07294e91f8938715fb8c1b861c5785129854a653c8c0e8f1c3da6581` |
| `1.0.34-4a7d51241424-download.json` | 9 | `e1d215eeea62374439a28d24081852b40a80f8231464a81f12e092d169b414d6` |
| `1.0.34-4a7d51241424-bundle.json` | 10 | `701d73f20dbd5c589d4a1ca827e8f039e511b78f806c5fc50a704a5c0f6fd370` |

Web 126/126、API 24/24、下载站 9/9 文件在本地、上传归档和远端未激活目录中逐一复验。API 白名单不含 `node_modules`、SQLite、data、备份目录、环境文件、PEM、P12、keystore 或其他秘密材料。

## 原生应用门禁

Android 包名为 `cn.dsponline.network`，版本为 `1.0.34 / 1000034`。APK Signature Scheme v2/v3、zipalign 和批准证书连续性通过；批准证书 SHA-256 为 `ede2aa09ed143a3fbeb283aad0e7801d192c851f240cd39278c399918a0216ce`。Android API 36 隔离模拟器完成正式 1.0.33 到 1.0.34 的 `install -r` 覆盖升级，`firstInstallTime` 保持，启动进程正常且无 Fatal/ANR。包内 API、更新源和公开 origin 分别为正式 HTTPS 地址。

Windows installer 和解包主程序的 FileVersion/ProductVersion、Build ID、正式 API 与 stable 更新基址通过；隔离用户目录启动通过。installer 与主程序 Authenticode 均为 `NotSigned`，下载页明确显示未知发布者提示。

## 发布前备份

两节点数据库始终相互独立，没有跨节点复制、合并、恢复、替换或初始化。

| 节点 | 备份 | 字节 | SHA-256 | 验证 |
| --- | --- | ---: | --- | --- |
| 香港 | `/var/lib/dsp-idle-cloud/backups/pre-1.0.34-20260808T110635Z.sqlite` | 2,516,602,880 | `42dfd8f150b200c820ee96c6b78fa370fc38bcda7dde329423380153bd646992` | mode `0600`、`quick_check=ok`、schema v7/layout v2 |
| 上海 | `/var/lib/dsp-idle-cloud/backups/pre-1.0.34-20260808T105418Z.sqlite` | 192,512 | `3356eca5d246631786982d7f3054e754b5b4447531fedd21d4bd4dcf15e184b7` | mode `0600`、`quick_check=ok`、schema v7/layout v2 |

香港首次在线 Backup API 在活跃写入下未收敛：有效文件停在 1,510,195,200 字节，但进程累计读取约 37.8 GB、写入约 91.8 GB。核对精确 PID、命令和目标路径后终止本次发布创建的备份进程，将目标标记为 `.incomplete`；在新的维护快照验证通过且确认无人占用后，只删除该无效文件。随后短暂停止健康重启定时器和云服务写入，使用同一 Backup API 在无写入条件下生成上表快照，并恢复原 1.0.33 服务与定时器；全过程 `NRestarts=0`。没有删除任何有效备份或生产数据库。

## 未激活目录验证

两地归档 SHA-256 与本地值一致后才解压到全新目录。两地 source manifest 均为 150/150；上海下载 manifest 为 9/9。新 API 目录分别执行 `npm ci --omit=dev`、生产依赖审计和服务测试，结果均为 52 项通过、2 项可选夹具跳过、0 失败，服务端生产依赖 0 漏洞。

香港和上海分别复制自己的已验证备份到临时隔离库，在 `127.0.0.1:14334` 启动 1.0.34；两地 health 均为 SQLite schema v7/layout v2。隔离进程停止后临时库、日志和端口均已清理，生产数据库未被隔离测试打开或修改。

## 原子切换与观察

发布顺序为香港 Web/API、上海 Web/API、上海下载页。两地 Web/API 使用固定 `/usr/local/sbin/dsp-idle-switch-release`，切换前停止健康重启定时器，切换工具完成 Nginx、服务和本机 health 后恢复定时器。上海下载页使用受保护状态文件和临时 symlink 原子切换。

香港服务启动后的短时重负载产生 85 个 502 和 60 个 504，最后一条为 2026-08-08 19:22:35 +08:00；服务未重启、journal 无 warning/error。负载收敛后，本机 15/15 health 约 1 ms，公网 15/15 约 0.36～0.76 秒；发布收口时最近 15/10/5 分钟 5xx 均为 0，因此没有回滚。上海 Web/API 连续 6/6 公网采样通过，DSP Web/API 和下载站近 30 分钟 5xx 均为 0。

两地服务和 `dsp-idle-healthcheck.timer` 均为 active，`NRestarts=0`。上海 `nginx -t` 继续报告发布前已存在、与 DSPidle2 无关的 server-name 冲突 warning，本轮未修改该虚拟主机。上传 staging 验收后删除；当前磁盘约为香港 69%（可用约 12 GB）、上海 83%（可用约 11 GB）。

## 公网验收

| 检查 | 结果 |
| --- | --- |
| 两地根页 / `version.json` | 200，Build ID `1.0.34+4a7d51241424` |
| 两地 `/api/health` | 200，schema v7、SQLite layout v2 |
| Origin | 香港允许 `https://dsponline.cn` 与 Android `https://localhost`，未知 origin 403；上海只允许受保护配置中的正式节点 origin，其他测试 origin 403 |
| Web 资源 | 当前 1.0.34 与上一版 1.0.33 JS/CSS 均可加载、gzip、immutable；HTML、version 和 service worker no-cache |
| 下载页 / feeds | 页面、Android stable JSON、Windows YAML/JSON 均为 1.0.34；Windows `NotSigned` 提示正确 |
| 完整公网文件 | 下载 manifest 9/9、共 108,214,957 字节完整流式下载，大小和 SHA-256 全部一致 |
| Range / 缓存 | APK、EXE、blockmap 均返回 `206`、1024 字节范围、正确 Content-Range；二进制 immutable，页面与 feeds no-cache |
| 香港下载入口 | APK、EXE、Android feed 均 302 到 `https://download.dsponline.cn`；`www` 为 301 到主域名 |
| 浏览器 smoke | Chrome 香港桌面/手机、上海桌面、下载页桌面/手机共 5/5；0 请求失败、0 页面错误、0 破图、0 横向溢出 |

没有使用真实生产账号执行云读取或写入，没有上传玩家存档或测试夹具。真实账号四槽写入、Android 物理设备长时上传与 Electron 内 20 MiB 长时结算仍是明确未验证项目。

## 当前指针与回滚

- 香港 Web/API：`web-1.0.34-4a7d51241424` / `api-1.0.34-4a7d51241424`
- 香港直接回滚：`web-1.0.33-2bd81de8d7f1` / `api-1.0.33-2bd81de8d7f1`
- 上海 Web/API：`web-1.0.34-4a7d51241424` / `api-1.0.34-4a7d51241424`
- 上海直接回滚：`web-1.0.33-2bd81de8d7f1` / `api-1.0.33-2bd81de8d7f1`
- 上海下载页：`download-site-1.0.34-4a7d51241424`
- 上海下载页直接回滚：`download-site-1.0.33-2bd81de8d7f1-r2`
- 下载页更深状态：`/var/www/dsp-idle-downloads/release-state/previous-release.pre-1.0.34-20260808T112559Z`，保留切换前的上一层状态

Web/API 回滚命令为各节点 `sudo /usr/local/sbin/dsp-idle-switch-release --rollback-last`。下载页按 `/var/www/dsp-idle-downloads/release-state/previous-release` 原子切回。所有回滚都只切换代码或下载指针，绝不恢复数据库。

## 剩余风险

- Windows 仍为 `NotSigned`，系统可能显示未知发布者或 SmartScreen。
- 没有物理 Android 设备；真机 1/2/7/20 MiB 云上传、锁屏、低内存、温度和耗电未验证。
- Electron 只完成正式包隔离启动，没有在 Electron 生命周期中运行 20 MiB 长时终局结算。
- 真实生产账号云读写未执行；不能把 health、隔离备份或浏览器匿名 smoke 描述为真实账号端到端通过。
- 香港启动大库时存在约 2～3 分钟短时高延迟/502/504 窗口；后续发布应继续停止健康定时器、扩大切换 health 等待，并在低流量窗口执行大库一致性备份。
- 上海磁盘仍处于 80% 告警区；不得删除当前版、直接回滚版或未证明已完成加密异地归档与哈希校验的有效备份。
