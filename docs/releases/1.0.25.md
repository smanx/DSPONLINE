# 1.0.25 / 画布交互、生产统计与设置体验

发布日期：2026-08-03
发布提交：`628369a93ad7c42fd17764471272da9c7dcbe917`
Release ID：`1.0.25-628369a93ad7`
构建 ID：`1.0.25+628369a93ad7`
GameState：v46；存档 envelope：v2；云 schema：v7；SQLite layout：v2

## 本次内容

- 建筑选中状态不再被 React Flow 的瞬时空选择清除，完整节点和低细节节点均保留明确选中反馈。
- 新增默认关闭的缺料自动跳转、按星球生产统计筛选和当前星球物理线路的上下游寻线模式。
- 自动保存增加 10 分钟和关闭选项；页面隐藏、退出与返回菜单的紧急保存不受周期关闭影响。
- 终局极限模式节点标题优先显示配方、主要产物或当前科技，未配置机器明确显示未设置配方。
- 设置页补齐窄屏和大字号布局，左右侧栏可独立收起并保留选择、标签页和滚动位置。
- 本版不升级 GameState、存档 envelope、云 schema 或 SQLite layout；代码回滚不恢复数据库。

## 发布门禁

| 检查 | 结果 |
| --- | --- |
| `npm ci` | 通过 |
| `npm run typecheck` | 通过，0 个 TypeScript 错误 |
| `npm test` | 76 个测试文件通过、1 个跳过；692 项通过、9 项跳过、0 失败 |
| `npm run test:server` | 46/46 通过；香港与上海未激活目录各自再次 46/46 |
| `npm run test:ops` | 6/6 通过 |
| `npm run test:native` | 8/8 通过 |
| `npm run licenses:check` | 128 个运行时包一致 |
| 生产依赖审计 | 0 个已知漏洞 |
| `npm run build` | 通过；Vite 转换 1,871 个模块 |
| `npm run test:e2e` | 218 项通过、6 项显式本地夹具/基准跳过、0 失败 |
| Release manifest | 本地与两地未激活目录均为 145/145 文件通过 |
| Manifest 聚合 SHA-256 | `de32de1b491a39f28559b727613dde61361c5fb760ecdd157806996390515982` |

全量 Playwright 首次运行发现 4 个公告与英文翻译回归，修复提交 `628369a93ad7` 后从 clean 候选完整重跑并通过。普通 Web 在原生构建后重新构建，除每次生成的 `version.json.generatedAt` 外其余 144 个文件与不可变清单一致；恢复候选原始 `version.json` 后 145/145 复验通过。

## 不可变制品

| 制品 | 大小 | SHA-256 | 说明 |
| --- | ---: | --- | --- |
| Source manifest | 145 个文件 | `de32de1b491a39f28559b727613dde61361c5fb760ecdd157806996390515982` | clean Git SHA `628369a93ad7c42fd17764471272da9c7dcbe917` |
| Web tar | 1,073,495 字节 | `bebd513880e8835cfdec5be1ee04d12e64cf5e6628a09b83f750f0e92a6fe1c3` | 123 个文件 |
| API tar | 74,662 字节 | `2100bc0148e4e8f2b2efb80c33caf9dc263d8e61fdd0989f0e7fda4371cc3ef1` | 22 个服务端/运维文件，不含数据库或 `node_modules` |
| 下载站 tar | 115,742,659 字节 | `eb90017800f241607586f838d93c261807c637c0b8d00a82bb4eb7160548c473` | 页面、清单、APK、EXE 和 blockmap 共 9 个文件 |
| Android APK | 4,589,531 字节 | `e5b74a6bc2022c5de5674b278bb21c8a135f1f6d35b1b323e1803b887b564e2b` | `1.0.25 / 1000025`，APK v2/v3 |
| Windows x64 EXE | 112,301,266 字节 | `bf0673e9ba9ad54759f1b4ecafa7be2e23dad318540280d8016bd6c43f3fff11` | FileVersion/ProductVersion 1.0.25，Authenticode `NotSigned` |
| Windows blockmap | 117,945 字节 | `0bfd2f6527fcaed61f82897efa73d40d83f6a6c0f038e1764c761b7dd1ff4519` | 与 `latest.yml` 同步发布 |

三个 tar 均在独立临时目录解包后与源目录逐文件比较，123/123、22/22、9/9 均无差异。Windows 标准 Electron 下载曾连接超时，仓库既有回退路径复用了本机完整 Electron 分发并成功打包；最终安装包的版本、Build ID、正式 API、稳定更新源、SHA-512、隔离启动和 `NotSigned` 状态全部独立复验通过。

## 原生签名与清单

- Android 包名为 `cn.dsponline.network`，versionName `1.0.25`、versionCode `1000025`、minSdk 24、targetSdk 36。
- APK Signature Scheme v2/v3 通过，批准长期证书 SHA-256 为 `ede2aa09ed143a3fbeb283aad0e7801d192c851f240cd39278c399918a0216ce`。
- APK 内置 `https://dsponline.cn/api`、`https://dsponline.cn/downloads/android/stable.json` 和 Build ID `1.0.25+628369a93ad7`，没有回环开发地址。
- Android `minimumSupportedVersionCode` 继续为 `1000002`。
- Windows 没有历史 Authenticode 证书。本版继续作为明确标记的未签名测试包发布，没有创建、替换或伪造证书。

## 香港 Web/API

- 发布前 Web/API 为 `1.0.24-019bac527829`，回滚状态为 `1.0.23-867e5101b689`。
- 发布前备份：`/var/lib/dsp-idle-cloud/backups/manual-20260803T104203Z-1.0.25.sqlite`，1,662,312,448 字节，SHA-256 `e3c208f6501c48e502649a8b11097ab5c7e2b3e4743b4c07c53d858cb60597db`，`quick_check=ok`。
- 备份摘要为 schema v7、账号 493、会话 831、主云档 410、修订 4,956、公开提交 405、玩家 4,766、反馈 8、错误聚合 1,000。
- 大库备份期间健康探针曾等待 60 秒超时，监控于 18:44:52 重启当时仍在线的 1.0.24 服务。备份已完整通过，旧服务随后连续 5/5 健康并稳定超过 8 分钟；没有恢复、替换或初始化数据库。
- 新目录 `/var/www/dsp-idle/releases/1.0.25-628369a93ad7` 与 `/opt/dsp-idle-cloud/releases/1.0.25-628369a93ad7` 完成归档哈希、145/145、46/46、生产依赖和备份副本 `14320` 隔离启动验证。
- 固定脚本使用 30 秒健康窗口原子切换成功。最终回滚点为 1.0.24，服务 `active/running`、`NRestarts=0`，发布后本机与公网健康持续通过。
- 鉴于发布前备份已触发过高 I/O，本轮没有在香港追加发布后在线手工备份；发布前一致性快照是明确恢复点。

## 上海 Web/API 与下载站

- 发布前 Web/API 与下载站均为 `1.0.24-019bac527829`，回滚状态为 1.0.23。
- 发布前备份：`/var/lib/dsp-idle-cloud/backups/manual-20260803T104203Z-1.0.25.sqlite`，143,360 字节，SHA-256 `8cdbd17845ec24a46c0c1ed5b5013b16f8c3d25bbeda6e79e8817ac870cfb9cd`，`quick_check=ok`。
- 发布后备份：`/var/lib/dsp-idle-cloud/backups/manual-20260803T110557Z-1.0.25-post.sqlite`，143,360 字节，SHA-256 `c3dd0c5600dd1e831a374969701f4de5a16eb5d798f5f634e7c986427ecb55a8`，`quick_check=ok`。
- 发布前后均为 0 个账号、0 个会话、0 个云档/修订/提交、27 条玩家记录和 23 条错误聚合，记录没有减少。
- Web/API 未激活目录通过 145/145、46/46、生产依赖和备份副本隔离启动后，由固定脚本原子切换；上海继续由本机静态目录和本机 `127.0.0.1:4320` 提供服务。
- 下载目录 `/var/www/dsp-idle-downloads/releases/1.0.25-628369a93ad7` 在未激活状态完成 9/9、清单和二进制复验后原子切换。
- Web/API 与下载站的独立回滚点均为 `1.0.24-019bac527829`。1.0.24 原生文件仍保留在上一下载目录。

## 公网验收

| 检查 | 结果 |
| --- | --- |
| 香港 / 上海根页与 API | 根页、`version.json`、`/api/health` 均为 200；Build ID `1.0.25+628369a93ad7`，schema v7、layout v2 |
| 香港 `www` | 301 到 `https://dsponline.cn/` |
| 上海独立性 | 上海正式 origin 保持本机 200，没有跳转香港；隔离 Chrome 中密码输入为 0 |
| Android Origin | `https://localhost` 为 200，未知 Origin 为 403 |
| Web 缓存 | 当前 JS/CSS 为 gzip + immutable；HTML/manifest no-cache，`sw.js` no-store |
| 历史 Web chunk | 1.0.24 独有 chunk 在两地均为 200、gzip、immutable |
| 主 JS 完整性 | 两地公网 SHA-256 均为 `f3d93128f222544f7bf1df875a4bc95eddc4fa8477b3833f87dba2f4d061e808`，与本地一致 |
| 下载页 | 页面、Build ID、两个下载按钮和 Windows 未签名提示正确；Android/desktop 清单 no-cache |
| APK 完整下载 | 4,589,531 字节，SHA-256 与清单一致；香港入口 302 到上海 |
| EXE 完整下载 | 112,301,266 字节，SHA-256 与清单一致；香港入口 302 到上海 |
| Range / binary cache | APK 与 EXE 均返回 206、`bytes 0-15/<完整大小>` 和 16 字节正文；二进制 immutable |
| 浏览器 | 香港桌面、上海 390x844、下载页桌面/390x844 共 4/4，无页面异常或横向溢出 |
| 观察窗 | 两地服务 `NRestarts=0`，健康 timer active；服务 journal 0 warning，DSP access log 最近 500 条 0 个 5xx |

生产截图位于 `artifacts/qa/production-1.0.25/`。浏览器上下文拦截了所有非 GET/HEAD API 请求，没有使用生产账号、上传测试存档或清理玩家浏览器数据。

## 回滚

- 香港 Web/API：`sudo dsp-idle-switch-release --rollback-last`，目标为香港 1.0.24 Web/API；只回滚代码，不恢复数据库。
- 上海 Web/API：同样执行固定脚本的 `--rollback-last`，目标为上海本机 1.0.24 Web/API。
- 上海下载站：将 `/var/www/dsp-idle-downloads/current` 原子切回 `/var/www/dsp-idle-downloads/releases/1.0.24-019bac527829`；受保护状态文件已经记录该路径。
- Android/Windows 回滚通过上海 1.0.24 下载目录恢复稳定清单和二进制。任何回滚均不恢复、替换或初始化数据库。

## 剩余风险

- Windows 安装程序仍为 `NotSigned` 测试包，Windows 可能显示未知发布者或 SmartScreen 提示。
- 本轮没有在实体 Android 设备上重新执行 1.0.24→1.0.25 覆盖升级；包名、递增 versionCode、v2/v3 和长期证书连续性已验证。
- 香港大库一致性备份可在活跃写入下阻塞健康接口。本轮发布前快照成功，但触发了一次健康探针重启；在完成低流量诊断前，不应在高峰连续创建多份手工快照。
- 当前下载站只通过 `current` 公开最新稳定文件；1.0.24 二进制保留在回滚目录，但旧版本公开 URL 不由当前目录继续提供。
