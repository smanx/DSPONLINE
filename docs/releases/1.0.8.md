# 1.0.8 存档完整性与性能诊断

> 日期：2026-07-28  
> 当前状态：本地发布候选  
> GameState：v39  
> 存档 envelope：v2  
> 云 schema：v7  
> SQLite layout：v2  
> Android：`1.0.8 / 1000008`

## 玩家变化

- 保存、导出和云上传前立即自检；服务端独立复核并拒绝内部校验异常的存档。
- 校验失败不再显示虚假的 0 分钟/0 实体/0 科技。结构完整的存档可以先备份原件，再连续确认两次执行救援。
- 物资配送枢纽三个输入端口可分别指定物资、恢复自动识别或清空。重置连接会保护性返还线路、施工件、缓存和在途物资。
- 亮色模式下建筑制造和物品手工制造的正常、选中、可制造、缺料及禁用文字保持清晰。
- 运营中心新增默认关闭的性能页，按需显示 FPS、主线程、Worker、积压、内存、存档、行星规模和模拟阶段耗时，并可导出匿名报告。

## 存档与确定性

v38→v39 只增加配送枢纽三端口状态和线路端口归属。迁移不重建实体、线路、物流、科研、制造、活动或戴森状态，也不改变任何已有库存。异常存档救援先迁移可解析状态，再以确切序列化对象生成新校验；只有复核通过才允许载入。

用户样本只读核验结果：原 envelope v2 / 状态 v38，记录校验 `27ca23f8`、重算 `4d328e3c`；结构未截断。救援前后均为 12,143.1581 秒、149 个实体、50 条线路和 30 项已完成科技，救援后为有效 v39。样本未上传、未覆盖或写回。

性能监控只在玩家主动开启后向 Worker 请求阶段计时。开启与关闭路径的状态哈希相同；样本只在当前页面保留最近 60 秒，不写入 GameState、本地存档或云存档。

## 本地验证

| 检查 | 结果 |
| --- | --- |
| TypeScript | 通过 |
| Vitest | 510 通过，2 个可选基准跳过 |
| Playwright | 162/162 通过，561.1 秒 |
| 云服务 | 36/36 通过 |
| 运维工具 | 6/6 通过 |
| 原生发布工具 | 6/6 通过 |
| 第三方许可证 | 128 个运行时包一致 |
| Vite 生产构建 | 通过 |
| `git diff --check` | 通过；仅既有行尾转换提示 |

关键截图位于：

- `artifacts/qa/v108-performance-desktop-1440x900.png`
- `artifacts/qa/v108-light-fabrication-1440x900.png`
- `artifacts/qa/v108-mobile-delivery-font200-390x844.png`
- `artifacts/qa/v108-classic-mobile-delivery-font200-390x844.png`
- `artifacts/qa/release-notes-2026-07-28-v108-360x480-font200.png`
- `artifacts/qa/release-notes-2026-07-28-v108-844x390.png`

## 发布保护

- Windows 与 Android 必须来自同一可追溯提交；Android 保持既有包名和长期签名，Windows 保持应用 ID。
- 在切换 Web/API 前分别读取两节点实际配置，并通过 SQLite Backup API 创建、验证备份。
- 新代码先上传未激活目录，复验清单、服务端、运维和生产备份副本隔离启动，再原子切换。
- 上海下载清单只在安装包签名、覆盖升级、文件大小、SHA-256 和公网完整下载全部验证后更新。
- 回滚只切换代码目录，不恢复、覆盖或初始化生产数据库。

生产 Release ID、Git SHA、manifest、安装包哈希、备份与公网证据将在实际发布完成后补记。
