# DSPidle2 1.0.33 发布记录

## 发布摘要

- 角色：release
- 目标：香港 Web/API、上海 Web/API、上海下载页、Android/Windows 稳定应用包
- 版本：`1.0.33`
- GameState：v46；存档 envelope：v2；云 schema：v7；SQLite layout：v2（均未变化）
- 应用源码 Git SHA：`2bd81de8d7f16040620378d37cb73649cf09dd17`
- Build ID：`1.0.33+2bd81de8d7f1`
- 下载页 favicon 修复提交：`262b4b43121b8435d6f095ed053a50fe352e7e35`
- Source manifest：149 个文件，聚合 SHA-256 `0f80274b72d24aed0f3060253db7e990c0efe2468aaba1916a14a6f303d268ef`
- 最终下载目录 manifest：9 个文件，聚合 SHA-256 `5a369e3d21181c5fb11635078a50889ecf0dcb6a28e68530d9b1a92102a78822`
- 最终 bundle manifest：10 个文件，聚合 SHA-256 `64a3a51c45b33c51f1c97aa5b08caae9c92696bb2bece57ee94d34bcf236eefc`

本版发布 `fast-30s-v2` 快速离线、`pure-idle-macro-v3` 时间扭曲纯挂机和 `time-warp-short-calibration-v3` 实时兼容路径。启动和恢复会刷新权威供电倍率；有限/无限科研使用共享整数与 BigInt 宏观账本跨越边界；普通合同、尾验、校准或 Worker 失败只转入有界保守宏观，不再整段精确重放。候选状态只有通过正式序列化、`inspectSave()` 和重载后才会提交。

## 制品、清单与签名

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| `web-1.0.33-2bd81de8d7f1-clean.tar.gz` | 1,314,779 | `3e9e514088e18fc292e20e8eb093426d2116107d7aa625ccaf0e5f1eaf906b98` |
| `api-1.0.33-2bd81de8d7f1-clean.tar.gz` | 80,917 | `e2898587ed0387f448eadaa621ca018a227f175c52042a2dff779b917c96fbc8` |
| `download-site-1.0.33-2bd81de8d7f1-r2.tar.gz` | 116,004,458 | `bb3d4d22bc3db5ad0c39a087739326d72799ff58286521ce35ecdf13ab0d0836` |
| Android APK `1.0.33 / 1000033` | 4,834,527 | `14232dd3273ad951acf36d0a97488912e978ae0cd6da3f5cf1104f82419bedeb` |
| Windows EXE `1.0.33` | 112,324,182 | `1fa4e4c32a53fbac1ed1c91112b27455e2b8745d954e969671b98165d24c761f` |
| Windows blockmap | 118,330 | `c8a1c66e95b4394b9c01eae8999847510fcae5bf8bd648e88afd424d9240ea7a` |

三份最终 manifest 均由仓库 verifier 重新读取实际文件验证：source `149/149`、download r2 `9/9`、bundle r2 `10/10`。Web/API/APK/EXE 仍绑定应用源码提交 `2bd81de8...`；favicon 声明只修改静态下载页模板并由提交 `262b4b4...` 追踪，没有重建或替换应用二进制。

Android 包名为 `cn.dsponline.network`，versionCode 为 `1000033`；APK Signature Scheme v2/v3 通过，证书 SHA-256 与 1.0.32 的批准长期证书连续。没有创建、替换或输出 keystore。Windows FileVersion/ProductVersion 为 1.0.33，安装包 Authenticode 为 `NotSigned`，下载页继续明确提示未知发布者。

## 开发与候选门禁

所有应用制品均从独立 clean worktree 的 source commit `2bd81de8...` 生成，没有使用或覆盖主工作树、stash、生产数据库或玩家存档。

| 检查 | 结果 |
| --- | --- |
| `npm ci` / `npm --prefix server ci` | 通过；服务端 0 个已知漏洞 |
| `npm run licenses:check` | 128 个运行时包一致 |
| `npm run typecheck` | 0 个错误 |
| `npm test -- --run` | 91 个文件通过、5 个跳过；825 项通过、16 项跳过、0 失败 |
| `npm run test:server` | 49/49 |
| `npm run test:ops` | 6/6 |
| `npm run test:native` | 8/8 |
| `npm run build` | 通过 |
| `npm run test:e2e` | 251 项通过、11 项显式可选夹具/故障注入跳过、0 失败 |
| Chrome / Edge 真实终局专项 | 两份只读夹具各 16/16 通过；源文件 SHA-256 未改变 |
| Android / Electron | Android 36.1 模拟器完成 1.0.28→1.0.32→1.0.33 覆盖启动；Electron 正式包隔离启动通过 |

大型真实存档的 30 天快速离线约 2.9～3.1 秒，30 天纯挂机约 2.8 秒。该误差口径是关键尾验估计，不是完整精确 30 天逐字段对照；保守宏观会冻结无法证明守恒的产线并少发收益。

## 备份、磁盘与未激活验证

两地发布前均运行 1.0.32，服务 active、`NRestarts=0`、schema v7、SQLite layout v2。生产数据库通过 Backup API 创建一致性备份，生产文件没有被直接复制、恢复、替换或初始化。

| 节点 | 备份 | 大小 | SHA-256 | 验证 |
| --- | --- | ---: | --- | --- |
| 香港 | `/var/lib/dsp-idle-cloud/backups/pre-1.0.33-20260807T110234Z.sqlite` | 2,403,254,272 | `5ce7c7d9e6368e03d93c99b42b28bb73b2484816fe2372f571de7e789c8868a6` | mode `0600`、`quick_check=ok`、schema v7/layout v2 |
| 上海 | `/var/lib/dsp-idle-cloud/backups/pre-1.0.33-20260807T110720Z.sqlite` | 184,320 | `a2792449f872f99ebc04b0d7894d87cb1f1cc94f2a31be5b601feb21aad7a4fa` | mode `0600`、`quick_check=ok`、schema v7/layout v2 |

未激活目录在切换前完成归档哈希、149 文件清单、生产依赖、49/49 服务端测试、6/6 运维测试和各自备份副本隔离启动验证。香港与上海继续使用独立数据库，上海没有代理香港 API。

香港磁盘清理只处理已证明可恢复的旧快照。删除前核对 2026-08-06 加密备份在 COS 与 staging 的大小和 SHA-256 完全一致，密文 SHA-256 为 `a1f2c11f19667d6d29235702705504f95ad79efd36a8a59cc6b08f3f8e6a2d32`；随后删除 1.0.25～1.0.30 的 6 份旧本地快照，共 11,068,690,432 字节。香港保留最新 4 份本地快照，生产数据库和异地备份均未删除或改写。发布后香港根盘约 62%、可用约 15.6 GB；上海约 83%、可用约 10.8 GB。

## 原子切换与公网验收

香港先切换 Web/API，上海随后独立切换相同不可变 release。两地服务和健康定时器均为 active，`NRestarts=0`；切换后 journal warning 均为 0。香港连续 6 次公网 health 均为 200，约 241～399 ms；上海连续 6 次均为 200，约 47～65 ms。

| 检查 | 结果 |
| --- | --- |
| 香港/上海根页与 `version.json` | 均为 200；Build ID `1.0.33+2bd81de8d7f1` |
| 香港/上海 `/api/health` | 200；schema v7、SQLite layout v2 |
| 上海独立性 | 本机 Web/API 独立提供服务，没有改为香港代理 |
| Web 静态资源 | 当前与 1.0.32 hashed assets 均可加载；JS/CSS gzip、immutable，HTML/version/service worker no-cache |
| 下载页和更新清单 | Android JSON、Windows YAML/JSON 均为 1.0.33；Windows `NotSigned` 提示正确 |
| 完整下载 | APK、EXE、blockmap 的公网大小和完整 SHA-256 与本地/清单一致 |
| Range/缓存 | APK、EXE、blockmap 均返回 Range `206`；二进制 immutable，页面和稳定清单 no-cache |
| 浏览器 smoke | 香港桌面/手机、上海桌面、下载页桌面/手机共五个场景，无 404、page error、资源失败、破图或横向溢出 |

初始 1.0.33 下载页浏览器 smoke 发现浏览器默认请求 `/favicon.ico` 返回 404。没有原地修改活动目录；模板改为显式声明 `/icon.svg` favicon，增加原生工具回归断言并提交为 `262b4b4...`。随后从新目录生成 download r2/bundle r2 清单并原子切换到 `download-site-1.0.33-2bd81de8d7f1-r2`；APK、EXE 和 blockmap 哈希保持不变，五个浏览器场景重新运行后错误归零。

两地 `nginx -t` 成功。上海仍报告发布前已存在、与 DSPidle2 无关的 server-name 冲突 warning；本轮没有改动该无关虚拟主机。

本轮没有使用真实生产账号执行云读取或云上传，也没有向生产数据库写入测试存档。API health、服务端完整测试、未授权边界和备份副本兼容已验证；真实账号端到端云写入仍是明确未验证项目。

## 当前指针与回滚

- 香港 Web/API：`web-1.0.33-2bd81de8d7f1` / `api-1.0.33-2bd81de8d7f1`
- 香港 Web/API 回滚：`web-1.0.32-762bf693becb` / `api-1.0.32-762bf693becb`
- 上海 Web/API：`web-1.0.33-2bd81de8d7f1` / `api-1.0.33-2bd81de8d7f1`
- 上海 Web/API 回滚：`web-1.0.32-762bf693becb` / `api-1.0.32-762bf693becb`
- 上海下载页：`download-site-1.0.33-2bd81de8d7f1-r2`
- 上海下载页直接回滚：`download-site-1.0.33-2bd81de8d7f1`
- 上海下载页更深回滚状态：`previous-release.pre-1.0.33-r2-20260807T113647Z`，指向 1.0.32-r2
- Web/API 回滚使用各节点 `sudo dsp-idle-switch-release --rollback-last`，只回滚代码并保留当前数据库
- 下载页按 `/var/www/dsp-idle-downloads/release-state/previous-release` 原子切换 symlink，不能恢复数据库

## 剩余风险

- Windows 仍为 `NotSigned`，系统可能显示未知发布者或 SmartScreen 提示。
- 没有物理 Android 设备；Android Chrome/WebView 上的 30 天真实存档、后台挂起、温度、耗电和低内存行为未验证。
- Electron 只完成正式包隔离启动，没有在 Electron 生命周期内运行 30 天真实存档。
- 真实生产账号云读取/写入没有执行，不能把 API health、未登录边界或备份副本测试描述为真实账号端到端云操作通过。
- 上海仍在 80% 磁盘告警区；不得删除当前版、回滚版或未证明已完成加密异地归档与哈希校验的有效备份。
