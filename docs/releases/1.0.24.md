# 1.0.24 / 工厂管理、历史堆叠兼容与终局画布

发布日期：2026-08-03
发布提交：`019bac5278290d5cf8e3ad2b3ac4a8389cdccef0`
构建 ID：`1.0.24+019bac527829`
GameState：v46；存档 envelope：v2；云 schema：v7；SQLite layout：v2

## 本次内容

- 建筑新增堆叠上限统一为一亿；历史存档中超过一亿但仍为安全整数的建筑、矿机和蓝图数量继续原样读取，不静默裁剪。
- 运营中心增加跨恒星系、行星和物流站的物流管理；远程编辑继续通过引擎域命令提交，不切换当前行星。
- 连接传送带后精确选中新建或增加并联的线路；建筑托盘增加单选、多选和框选回收；物品卡增加定位与图鉴快捷操作；两套手机界面补齐蓝图导入。
- 画布 P0-P6 合并进正式代码。普通模式使用当前星球轻量投影、拓扑缓存和空间索引；终局极限模式仍为设备级可选项并保留 React Flow 回退。
- P6 多 Worker 路径已完成确定性与守恒验证，但真实终局档相对单 Worker 仍慢约 40%～73%，因此生产构建继续硬关闭。
- 本版不升级 GameState、存档 envelope、云 schema 或 SQLite layout，代码回滚不恢复数据库。

## 发布门禁

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过，0 个 TypeScript 错误 |
| `npm test` | 76 个测试文件通过、1 个跳过；689 项通过、9 项跳过、0 失败 |
| `npm run test:server` | 46/46 通过 |
| `npm run test:ops` | 6/6 通过 |
| `npm run test:native` | 8/8 通过 |
| `npm run licenses:check` | 128 个运行时包一致 |
| `npm run build` | 通过；Vite 转换 1,871 个模块 |
| `npm run test:e2e` | 217 项通过、6 项显式夹具/基准跳过、0 失败 |
| Release manifest | clean worktree 与两地未激活目录均为 145/145 文件通过 |
| Manifest 聚合 SHA-256 | `be7b9d051a289b38e669d0174ca524a8f421b6a725dcc8604dd6f4bade422a14` |

两台服务器的未激活 API 目录分别执行 `npm ci --omit=dev` 和 46/46 服务端测试。香港使用 1,614,831,616 字节的生产备份副本、上海使用 135,168 字节的生产备份副本，在 `127.0.0.1:14320` 隔离启动；两地均返回 schema v7 / SQLite layout v2，测试副本和日志随后删除。

## 不可变制品

| 制品 | 大小 | SHA-256 | 说明 |
| --- | ---: | --- | --- |
| Web tar | 1,068,278 字节 | `03ec8bee4f36091383e2b37b6ef50ed14c48d57b1882ffe54cf5ed247728022f` | 123 个发布文件 |
| API tar | 74,556 字节 | `f9c7ce9b808e2fedebf6e786e13f9fe0794cd00a0b73e52528b03df12d0f5264` | 22 个服务端/运维文件，不含数据库或 `node_modules` |
| 下载站 tar | 115,736,141 字节 | `2af3f18163bb8b048aacb756a49853fb28624c111dca5c3d390f874e5a01b5e6` | 页面、更新清单、APK、EXE 和 blockmap |
| Android APK | 4,584,404 字节 | `e5761b3b9cab5b635c3c2d9d2f5a86c4b86c5c4ec6d54c0a03599b6c82da455a` | `1.0.24 / 1000024`，APK v2/v3，批准长期证书指纹 `ede2aa09ed143a3fbeb283aad0e7801d192c851f240cd39278c399918a0216ce` |
| Windows x64 EXE | 112,309,470 字节 | `c3b8e68abffe68a2dfd6627c128ed6a0cf77065a05dcc9210bb533800ab8e3b0` | FileVersion/ProductVersion 1.0.24；Authenticode `NotSigned` |

Windows 没有历史 Authenticode 证书。本版沿用既有策略发布明确标记为未签名的测试包，没有创建、替换或伪造证书。

## 香港 Web/API

- 发布前指针：`/var/www/dsp-idle/releases/1.0.23-867e5101b689`、`/opt/dsp-idle-cloud/releases/1.0.23-867e5101b689`。
- 发布前备份：`/var/lib/dsp-idle-cloud/backups/manual-20260803T053705Z-1.0.24.sqlite`，1,614,831,616 字节，SHA-256 `0bb0ae77486b8658e4d038a14c7c14ada55478b8f0267f263690f6f05398f742`，`quick_check=ok`。
- 新目录：`/var/www/dsp-idle/releases/1.0.24-019bac527829`、`/opt/dsp-idle-cloud/releases/1.0.24-019bac527829`。
- 原子切换使用 30 秒健康窗口，约 19 秒后成功；最终服务 `active/running`、`NRestarts=0`，本机 API 连续 5/5 约 1 ms，公网 API 连续 3/3 为 200。
- `/var/lib/dsp-idle-cloud/release-state/previous-release` 保存 1.0.23 Web/API 目录作为代码回滚点。
- 切换后尝试创建第二份手工快照时，在线 Backup API 在高写入活动下没有收敛：目标大小停在 1,077,248,000 字节，累计写入约 82 GB，并使健康检查短暂超时。已核对精确 PID 和目标路径后终止该备份进程，确认无人占用后仅删除不完整快照；生产数据库、发布前有效备份和服务均未删除、替换或恢复。释放 I/O 后本机 5/5、公网 3/3 健康检查恢复，`NRestarts=0`。本轮不再在活跃流量下重试香港发布后备份。

## 上海 Web/API 与下载站

- 发布前 Web/API 和下载站均为 `1.0.23-867e5101b689`。
- 发布前备份：`/var/lib/dsp-idle-cloud/backups/manual-20260803T053705Z-1.0.24.sqlite`，135,168 字节，SHA-256 `3762f7b343e7f2368d599cec6e69ec8391d34c82fc9f9f9dfd8131ff2f6b5958`，`quick_check=ok`。
- 新 Web/API 目录：`/var/www/dsp-idle/releases/1.0.24-019bac527829`、`/opt/dsp-idle-cloud/releases/1.0.24-019bac527829`。
- 新下载目录：`/var/www/dsp-idle-downloads/releases/1.0.24-019bac527829`。
- Web/API 与下载页分别原子切换；上海继续由本机静态目录和本机 `127.0.0.1:4320` 提供服务，没有代理或跳转到香港。
- Web/API 回滚指针与下载站独立回滚指针均为 1.0.23。服务 `active/running`、`NRestarts=0`；既有其他站点的 `server_name` 冲突警告仍存在，但 `nginx -t` 成功。
- 发布后备份：`/var/lib/dsp-idle-cloud/backups/manual-20260803T064101Z-1.0.24-post.sqlite`，135,168 字节，SHA-256 `334690fa2418571f568dffc23f11b46e66a88698cc700191ae517134c99db2d9`，Backup API 的 `quick_check` 通过。

## 部署规则同步

现场检查发现两地固定切换脚本和应用静态规则仍是旧版：上传边界为 9 MiB，且没有保留上一版本 hashed assets。发布收尾时完成以下可回滚同步：

- 两地 Nginx 应用规则改为 `client_max_body_size 40m`，匹配当前服务端合法上传边界。
- 1.0.23 和 1.0.24 hashed assets 共 148 个文件复制到 `/var/www/dsp-idle/shared/assets`；旧版专属 chunk 在香港 HTTPS 和上海 HTTP 均返回 200、gzip、immutable。
- 两地 `/usr/local/sbin/dsp-idle-switch-release` 更新为 clean 提交中的版本，SHA-256 为 `5fdd497846cd3851db3ce0a44c4eebcfd21480ba4139b37a56215691bb9b7b37`，后续切换会自动归档当前和目标 release 的 hashed assets。
- 香港公共 snippet 与上海独立站点配置均先创建 `pre-1.0.24-20260803T065500Z` 备份；安装后 `nginx -t`、reload、版本与 API 健康复验通过。没有替换上海下载站、其他站点或 TLS 配置。

## 公网验收

| 检查 | 结果 |
| --- | --- |
| 香港根页 / 上海根页 | `https://dsponline.cn/` 与 `http://111.229.128.211/` 均 200 |
| 两地 `version.json` | `1.0.24` / `1.0.24+019bac527829` |
| 两地 `/api/health` | 200；schema v7、SQLite layout v2；服务 `NRestarts=0` |
| 香港 `www` | 301 到 `https://dsponline.cn/` |
| Android Origin | `https://localhost` 连续 3/3 为 200，未知 Origin 为 403 |
| Hashed JS/CSS | 200、gzip、`public, max-age=31536000, immutable` |
| `sw.js` / manifest | 200；`sw.js` no-store，manifest no-cache |
| 下载页面与清单 | 1.0.24、Windows 未签名提示正确；Android/desktop 清单 no-cache |
| Range | APK 与 EXE 均返回 206、`bytes 0-15/<完整大小>`，响应长度 16 字节 |
| 完整 APK 下载 | 4,584,404 字节，SHA-256 与清单一致；香港入口 302 到上海下载域名 |
| 完整 EXE 下载 | 112,309,470 字节，SHA-256 与清单一致 |

## 回滚

- 香港 Web/API：`sudo dsp-idle-switch-release --rollback-last`，目标为 1.0.23 Web/API，只切代码，不恢复数据库。
- 上海 Web/API：同样执行 `sudo dsp-idle-switch-release --rollback-last`，目标为上海本机 1.0.23 Web/API。
- 上海下载站：将 `/var/www/dsp-idle-downloads/current` 原子切回 `/var/www/dsp-idle-downloads/releases/1.0.23-867e5101b689`；该路径也保存在 `/var/lib/dsp-idle-cloud/release-state/previous-download-release`。
- Nginx/切换工具：两地均保留带 `pre-1.0.24-20260803T065500Z` 后缀的现场配置与工具备份。
- 任何代码、下载页或 Nginx 回滚均不恢复、替换或初始化数据库。

## 剩余风险

- Windows 安装程序仍为 `NotSigned` 测试包，Windows 可能显示未知发布者或 SmartScreen 提示。
- 本轮没有在实体 Android 设备上重新执行覆盖升级；包名、递增 versionCode、v2/v3 签名和长期证书连续性已验证。
- 香港大库的发布后在线手工快照在活跃写入下出现高 I/O 不收敛，已安全中止；发布前一致性备份仍是本轮明确恢复点。后续应在低流量窗口诊断 Backup API 的页面重试行为，不应在生产高峰直接重跑。
- 上海 Nginx 的既有其他站点 `server_name` 冲突警告仍需独立治理，本次没有修改那些站点。
