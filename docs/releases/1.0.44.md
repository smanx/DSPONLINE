# DSP极简网络 1.0.44 完整稳定发布记录

> 发布日期：2026-08-17（香港/上海 Web+API、上海下载页、Windows/Android stable 全量发布）
> 发布范围：香港 Web + API、上海 Web/API、上海下载页、Windows stable、Android stable（六目标全部覆盖）
> 最终源码提交：`3e580c715a5a07d828cfe78faf6648d2a2cafe43`
> Release ID：`1.0.44-3e580c715a5a`
> Build ID：`1.0.44+3e580c715a5a`
> Android：`cn.dsponline.network` versionName `1.0.44` / versionCode `1000044`，APK v2/v3 签名 + 历史证书连续性验证通过

## 发布结论

1.0.44 已在全部六项目标上完成稳定发布并通过验证。GameState v46、envelope v2、云 schema v7、SQLite layout v2 与 IndexedDB records 均不变；本次为代码级稳定发布，无 schema/layout 迁移，回滚不恢复数据库。

| 目标 | 状态 |
| --- | --- |
| 香港 Web | ✅ `web-1.0.44-3e580c715a5a` 在线 |
| 香港 API | ✅ `api-1.0.44-3e580c715a5a` 在线 |
| 上海 Web | ✅ `web-1.0.44-3e580c715a5a` 在线 |
| 上海 API | ✅ `api-1.0.44-3e580c715a5a` 在线 |
| 上海下载页 | ✅ `download-site-1.0.44-3e580c715a5a` 在线 |
| Windows stable | ✅ `dsp-idle-1.0.44-x64-setup.exe` 发布 |
| Android stable | ✅ `dsp-idle-1.0.44-1000044.apk` 发布 |

## 香港（正式节点）

- Web 目录：`/var/www/dsp-idle/releases/web-1.0.44-3e580c715a5a`（148 文件，远端 manifest 复算一致，aggregate `76edde1b…`）
- API 目录：`/opt/dsp-idle-cloud/releases/api-1.0.44-3e580c715a5a`（162 文件，远端 manifest 复算一致，aggregate `1bf2dcc6…`）；`npm ci --omit=dev` + better-sqlite3/ses SDK 验证通过
- 原子切换：generation 13 → **14**；current Web/API 均 `1.0.44`，slot **green / 4322**；previous（回滚目标）Web `web-1.0.43-fceca3eda51c`、API `api-1.0.42-c24e6247d257`
- 发布代理：`api-proxy.json` generation 29 → **32**，mode `forward` → `127.0.0.1:4322` slot green `api-1.0.44`
- 活动 API：`dsp-idle-api-active.service` active，MainPID 8594，`NRestarts=0`；`pending-switch.json` 不存在
- 备份与 evidence：`/var/lib/dsp-idle-cloud/release-state/pre-1.0.44-3e580c715a5a-backup-evidence.json` 与 `-preflight-evidence.json` 均通过（quick_check=ok、schemaVersion 7、storageLayoutVersion 2；快照 `manual-pre-1.0.44-3e580c715a5a-v2.sqlite` 3,730,227,200 B，sha256 `1cbcfdb7…`）
- 公网验证（`https://dsponline.cn`）：`/`、`/version.json`（`1.0.44+3e580c715a5a`）、`/api/health`（schema v7/layout v2/backup idle）、`/api/ready`（writable=true/pendingWrites=0）、`/sw.js`、`/manifest.webmanifest`、hashed asset `assets/index-DWSOAOE3.js` 全部 200
- 缓存/范围头：入口 `Cache-Control: no-cache,no-store,must-revalidate`；hashed asset `public, max-age=31536000, immutable`；`Range: bytes=0-99` → `206`
- node-health：切换瞬间有一次瞬时 `endpoint:/api/health` 失败（drain 期），随后自愈 `ok=true`、无失败
- 磁盘：VPS 扩容后 69G，54% 已用 / 31G 可用（远低于 90% 硬保护线）

## 上海

- Web/API 目录同香港命名：`web-1.0.44-3e580c715a5a` / `api-1.0.44-3e580c715a5a`（远端 manifest 复算 148/148、162/162 一致）
- Web/API 原子切换：generation 4 → **5**；current Web/API 均 `1.0.44`，slot **blue / 4321**；previous（回滚目标）Web `web-1.0.42-c24e6247d257`、API `api-1.0.42-c24e6247d257`
- API/代理：`dsp-idle-api-active.service` active（MainPID 2728322）、代理 active；`api-proxy.json` generation 10 `forward` → `127.0.0.1:4321` slot blue `api-1.0.44`
- 备份与 evidence：`pre-1.0.44-3e580c715a5a-backup-evidence.json` 与 `-preflight-evidence.json` 均通过（schema v7 / layout v2；上海市快照 401,408 B，sha256 `02ea447b…`；0 账号/43 玩家/0 报错正常）
- 公网验证（`http://111.229.128.211`）：`/`、`/version.json`（`1.0.44+3e580c715a5a`）、`/api/health`、`/api/ready`、`/sw.js`、`/manifest.webmanifest` 全部 200；页面标题 `DSP极简网络`
- node-health：`ok=true`、failedChecks 空
- 磁盘：77% 已用

## 上海下载页 & Windows/Android stable

- 下载节点为上海（`download.dsponline.cn` → `111.229.128.211`），非香港。
- 新目录：`/var/www/dsp-idle-downloads/releases/download-site-1.0.44-3e580c715a5a`（9 文件，远端 manifest 复算 9/9 aggregateOk；version `1.0.44+3e580c715a5a`）
- 原子切换：`current` → `download-site-1.0.44-3e580c715a5a`；回滚指针 `previous-download-release` = `download-site-1.0.42-c24e6247d257`
- Nginx：active，`nginx -t` OK；443 root 指向 `/var/www/dsp-idle-downloads/current`
- 公网验证（`https://download.dsponline.cn/`，标题 `DSP极简网络客户端下载`）：
  - `/version.json`、`/downloads/android/stable.json`、`/downloads/desktop/stable/latest.yml`、APK、EXE 均 HTTP 200
  - `stable.json`：versionName `1.0.44`、versionCode `1000044`、APK sha256 `74e98eb31fceb4a798d7b3d6f41693543310534f3c9440596f92ae29a29d0989`、size 5090402（与开发交接一致）
  - `latest.yml`：version `1.0.44`、EXE size 103408911、sha512 `Os2ZLyTA2ZqU3SFiWE2DXoV0/6BVeN0FuuRUhhtVlUwvR9AQr4G2Tb5dBEgnyCDUfmjC4TZ0uP7ASwYkgqgIJg==`

## 源码与制品

- Git 工作树干净；Six sub-manifests 全部远端复算一致（web / api / download-site）。
- Android 签名：APK v2/v3 + 获批历史证书连续性验证通过（见开发交接）。
- Windows 安装包维持历史公开策略为 NotSigned（已知残余风险，非本次回归）。

## 备份证据与数据

- 两地切换前均创建并通过宽松一致性 SQLite Backup API 快照 + evidence：香港 3,730,227,200 B（schema v7/layout v2），上海 401,408 B（schema v7/layout v2）。
- 未删除、未改动任何 live DB、WAL、SHM、历史有效备份或 COS 归档；第三次 COS 快照（`manual-pre-1.0.44-3e580c715a5a.sqlite` 于 `/lhcos-data/dsp-idle-archive/auto/`）保留为归档备份，未删除。

## 回滚

- 香港：只在只读复核仍为 generation 14 current 1.0.44 / previous 1.0.43(Web)+1.0.42(API)、pending 为空时，用已验证 evidence 执行 `--rollback-last`；预期回到 current 1.0.43/1.0.42。绝不恢复数据库。
- 上海：只在复核仍为 generation 5 时执行 `--rollback-last`，回到 1.0.42。
- 下载页/原生：`current` 切回 `download-site-1.0.42-c24e6247d257`。

## 操作说明（本次执行的安全要点）

- 使用本机已批准 SSH 凭据（香港 `D:\360安全浏览器下载\1111.pem`、上海 `refidle_alpha_ed25519`），物理出口绑定 `192.168.0.210`，严格 host-key/TLS 校验；未输出/复制密钥、token、主机敏感信息或玩家存档。
- 香港 API 冷启动采用 300s readiness 窗口；大库 evidence 在快照/预置副本阶段完成完整 SHA，切换关键路径只复核身份与元数据。
- 无测试账号/测试存档写入生产。

## 残余风险与后续运维

- **香港异地加密备份服务**：`dsp-idle-offsite-backup.service` 近几日因 COSFS 写路径（ENOSPC / EROFS / EIO）多次失败，最后成功每日 `.dspbak` 为 08-14。本次 1.0.44 未纳入修复（COS 容量充足，与代码发布无关，避免占用发布窗口），记录为已知残余风险，需后续单独排查 COSFS 写路径（直写 COS 或 rclone）并恢复每日异地加密备份。
- **上海恢复演练服务**：`dsp-idle-restore-drill.service` 上月末显示 failed，属月度演练，不阻塞发布，需后续核对其 timer/脚本是否正常。
- **Windows 安装包**：维持历史公开策略为 NotSigned（非本次回归，见开发交接残余风险）。
- **第三次 COS 快照 evidence**：第三次香港快照已写入 COS、但本次未补全其 evidence JSON；其本身仅作归档保留（未删除），本次发布已用新建快照的完整 evidence 完成切换，不影响本版证据链。
