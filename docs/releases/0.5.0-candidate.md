# DSP极简网络 0.5.0 候选版更新报告

> 候选日期：2026-07-23  
> 应用版本：0.5.0  
> 游戏状态：v30  
> 云服务 schema：v6（未变化）  
> 发布状态：仅本地完成，香港和上海仍运行 0.4.0 / v28

## 1. 本次范围

### 移动端、交互与性能

- 双指可从建筑、传送带或密集区域接管画布平移与缩放，第二指会取消第一指未提交的拖动、连线、采矿、放置和长按。
- 游戏区域拦截浏览器原生右键与系统长按菜单，同时保留输入框编辑、游戏内右键拖动和长按快捷菜单。
- 粗指针设备降低画布状态发布频率并默认关闭高耗能常驻效果；全屏工作区和页面后台期间冻结底层画布刷新。
- 科技树滚轮与触控板手势固定为横向移动，到达边界后不向外层页面泄漏纵向滚动。

### 建造、回收与画布管理

- 灰色快速建造锤改为单击上游缺料定位，锤子颜色和实际制造共用同一份完整材料链规划。
- 采矿机、原油萃取站和抽水站可按数量回收；矿脉、储量、缓存、生产进度和线路物资保留。
- 已堆叠普通建筑增加 `-1`，逐台返还施工库存；最后一台进入原完整拆除流程。
- 建筑制造中心使用真实约 2 倍节点尺寸和独立巨构状态效果，端口不依赖 CSS 缩放。
- 生产区域新增四边与四角共 8 个缩放手柄，桌面指针和手机触摸均可调整尺寸。

### 星际物流与资源

- 星际物流站可从所在行星物资托盘自动补充空间翘曲器，并设置目标库存；多个站点不会重复扣料。
- 新建到物资配送枢纽的输入线路默认低优先级，玩家手动设置和旧存档线路优先级保持不变。
- 澜渊 II、赤砂 I 和牧云 II 的原油资源按稳定 ID 无损补充；已有油井、矿储和自定义节点不覆盖。
- 殖民卡片固定显示物资来源、当前数量、需求数量及阻塞原因。

### P0 本地存档急救

- `productionHistory` 只保留在运行内存，不再写入主存档、备份后的新版本、手动槽位或快照；刷新后统计曲线重新积累，不影响工厂进度。
- 自动快照从 5 份调整为 2 份，间隔从 30 秒调整为 5 分钟；手动快照和三个手动槽位不参与自动清理。
- 首次保存检测到带生产历史的旧自动快照时只保留最新一份，其余旧自动快照会被清理。
- 保存顺序改为轻量化、清理自动快照、写主存档、读回校验、尽力备份上一版本、最后创建自动快照。
- `QuotaExceededError` 只会从最旧自动快照开始清理并重试主存档一次，绝不自动删除手动数据。
- 只有写入与读回校验成功才显示“主存档已保存”；最终失败会持续显示红色告警和“立即导出当前进度”。

## 2. 真实存档验收

使用玩家提供的 `dsp-idle-save-2026-07-22.json` 只读验收：

| 项目 | 结果 |
| --- | --- |
| 原文件 | 1,365,050 字节，v26，180 条生产历史 |
| 保存后 | 210,764 字节，约 205.8 KiB，v30 |
| 实体 | 原 189 个全部保留；按既有规则新增 2 个确定性油井 |
| 传送带 | 119 条保持一致 |
| 其他进度 | 库存、施工库存、科研、物流与戴森状态逐项一致 |
| 唯一重置项 | 生产统计曲线历史，刷新后重新积累 |

测试仅使用隔离的 jsdom `localStorage`，没有修改浏览器现有存档，也没有把附件内容或路径加入仓库。

## 3. 自动化结果

| 检查 | 结果 |
| --- | --- |
| TypeScript | 通过 |
| Vitest | 27/27 文件，268/268 通过 |
| 云服务 | 22/22 通过 |
| 运维工具 | 5/5 通过 |
| Vite 生产构建 | 通过 |
| Playwright Chrome | 97/97 通过，约 5.1 分钟 |

浏览器回归包含保存失败急救导出、保存恢复后告警消失、自动/手动快照隔离、手机生产区域触摸缩放、双指接管、200% 字体、线路、物流、科研和银河终局。

## 4. 发布门禁与下一步

- 尚未完成 Android Chrome 与 iPhone Safari 各连续 30 分钟的温度、耗电、帧率和卡顿测试；桌面 Chrome 触摸仿真不能替代真机结论。
- 尚未创建 Git 提交、正式标签、发布 manifest 或生产数据库备份。
- 尚未部署香港或上海节点，线上玩家继续使用 0.4.0 / v28。
- 用户本地验收通过并明确要求发布后，再按“生产数据库备份 → 单节点灰度 → 健康与存档烟测 → 第二节点 → 观察与回滚点确认”执行。
