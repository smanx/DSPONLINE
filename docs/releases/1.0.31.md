# DSPidle2 1.0.31 发布记录

## 发布摘要

- 角色：release
- 目标：香港 Web/API、上海 Web/API、上海下载页、Android/Windows 稳定应用包
- 版本：`1.0.31`
- GameState：v46；存档 envelope：v2；云 schema：v7；SQLite layout：v2（均未变化）
- Git SHA：`19040a9d1e453677fde0905d9576bb7b74ae0ec0`
- Build ID：`1.0.31+19040a9d1e45`
- 发布清单：148 个文件，聚合 SHA-256 `26128cdc8490fac12f99e617d67ff3a8d27f1b4024fefb0c30d2761242c8603f`

本版完成 2026-08-05 玩家反馈批次：修复离线报告布局、快速离线循环游标异常、取消科技后的队列续研、锁定建筑配方拓扑和零 tick 线路数量；增加有界生产历史曲线、施工库存删除、物品悬浮开关、移动统计滚动和高倍率纯挂机近似治理。设备级 UI 偏好不进入 GameState、导入导出、状态哈希或云存档。

## 制品与签名

| 制品 | 大小 | SHA-256 |
| --- | ---: | --- |
| `release/web-1.0.31-19040a9d1e45-clean.tar.gz` | 1,176,872 | `aee635577c3beb22b792a38df0c8950c625f75dfd0179f7431d2c37f0f163380` |
| `release/api-1.0.31-19040a9d1e45-clean-r2.tar.gz` | 80,945 | `80a3d40ec70e3b7daa92d55a0cbfbc482ac88426763556eb676734c1b340fcdb` |
| `release/download-site-1.0.31-19040a9d1e45.tar.gz` | 115,842,693 | `45d68628d6f11c5001185c1d45f1ab3ba1786e88f1c7bbaaf641e75f38ec52f8` |
| Android APK `1.0.31 / 1000031` | 4,696,436 | `e59e2c0d6d1e3042af35b2429d2a46ae5da592694b15f9210ac4ee79f72e0120` |
| Windows EXE `1.0.31` | 112,302,079 | `5f5426115fbe3a128af25dd7737f43976e3569f872b3051088ef950186a23a7c` |
| Windows blockmap | 117,994 | `8c1b999cd44b63fe1cddbd8e4ee1732766bf82f2e2bc2a3c5eba41afd48642c1` |

Android 包名为 `cn.dsponline.network`，versionCode 为 `1000031`；APK Signature Scheme v2/v3 和 1.0.0 至 1.0.30 的长期证书连续性通过。Windows FileVersion/ProductVersion 为 1.0.31，Authenticode 继续按历史策略明确标记 `NotSigned`，没有创建新证书。

## 开发与候选门禁

候选来自独立 clean worktree `codex/release-1.0.31`，共享主工作区的其他 Agent 未提交内容未被用于构建或修改。

| 检查 | 结果 |
| --- | --- |
| `npm ci` / 服务端依赖 | 通过；生产依赖安装和审计 0 漏洞 |
| `npm run typecheck` | 0 个错误 |
| `npm test -- --run` | 87 个文件通过、5 个跳过；778 项通过、16 项跳过、0 失败 |
| `npm run test:server` | 本地及两地未激活目录均为 49/49 |
| `npm run test:ops` | 6/6 |
| `npm run test:native` | 8/8 |
| `npm run licenses:check` | 128 个运行时包一致 |
| `npm run build` | 通过；Vite 转换 1,878 个模块 |
| `npm run test:e2e` | 246 项通过、2 项显式可选夹具/故障注入跳过、0 失败 |
| 发布清单 | 本地及两地未激活目录均为 148/148 |
| `git diff --check` | 通过 |

真实只读存档的 10 分钟、1 小时、7 天和 30 天快速离线均完成且源文件 SHA-256 不变；8x/12x/16x 各执行 1,800 秒虚拟墙钟、225 个连续切片，实际推进倍率分别为 8/12/16，浏览器 Worker 停止约 256 ms 且无迟到消息。快速路径仍允许既定的近似偏差和安全回退，不能描述为全状态精确或任意设备 30 秒硬保证。

## 备份与未激活验证

发布前两地均运行 `1.0.30-c6d896ae6911`，服务 active、`NRestarts=0`、schema v7、SQLite layout v2。两地分别通过 SQLite Backup API 创建一致性备份；生产数据库没有被复制到另一节点、恢复、替换或初始化。

| 节点 | 备份 | 大小 | SHA-256 | 验证 |
| --- | --- | ---: | --- | --- |
| 香港 | `/var/lib/dsp-idle-cloud/backups/pre-1.0.31-20260806T0750Z.sqlite` | 2,113,228,800 | `3986be338019b4394fad8efcc617a5af1145533f172910a32adbe5be8985dad2` | mode `0600`、`quick_check=ok`、schema v7 |
| 上海 | `/var/lib/dsp-idle-cloud/backups/pre-1.0.31-20260806T0755Z.sqlite` | 176,128 | `733484c3dbe2d8c7dde78743f71e6fed6f530f370cd93fe6d4e50dd35b81725f` | mode `0600`、`quick_check=ok`、schema v7 |

香港大库备份期间健康监控曾重启仍运行 1.0.30 的旧服务；旧版恢复 active、健康正常且 `NRestarts=0` 后才继续发布。两地新 API 均使用各自备份的临时副本在隔离端口启动，返回 schema v7/layout v2；临时副本和进程随后清理。

未激活目录：

- 香港 Web/API：`/var/www/dsp-idle/releases/web-1.0.31-19040a9d1e45`、`/opt/dsp-idle-cloud/releases/api-1.0.31-19040a9d1e45`
- 上海 Web/API：同名独立目录，继续使用上海本机 API 与独立数据库
- 上海下载页：`/var/www/dsp-idle-downloads/releases/download-site-1.0.31-19040a9d1e45`

三处归档在服务器复验后才激活。两地 API 目录均完成生产依赖安装、49/49 服务端测试和备份副本隔离启动；下载目录 8 个页面、清单和二进制文件全部复验。

## 原子切换与公网验收

香港先切换并完成观察，再切上海 Web/API，最后单独切换上海下载站。两地 Web/API 使用固定 `dsp-idle-switch-release` 工具；下载站使用临时 symlink 和 `mv -Tf`。每次切换前后 `nginx -t` 均成功；上海仅出现已有的无关 server-name 冲突警告。

| 检查 | 结果 |
| --- | --- |
| 香港/上海根页与 `version.json` | 均为 200；Build ID `1.0.31+19040a9d1e45` |
| 香港/上海 `/api/health` | 200；schema v7、SQLite layout v2 |
| systemd | 两地 `active/running`、`NRestarts=0` |
| 香港别名 | `www` 301 到 `https://dsponline.cn/` |
| 上海独立性 | 上海 HTTP 根页和本机 API 为 200，不跳转或代理香港；隔离 Chrome 中密码输入为 0 |
| Web 资源 | 新 JS/CSS 与 1.0.30 历史 JS/CSS 在两地均为 200、gzip、immutable；HTML、manifest 和 `sw.js` 保持 no-cache |
| Cloud/CORS 边界 | `https://localhost` PUT 预检 204；未知 Origin 403；未登录云读/写均为 401 |
| 下载页 | 页面、Android JSON、桌面 YAML/JSON 均为 1.0.31；两个按钮和 Windows `NotSigned` 提示正确 |
| 完整下载 | APK、EXE、blockmap 的公网完整下载大小和 SHA-256 与本地/清单一致 |
| Range/缓存 | APK 与 EXE 均为 `206 bytes 0-15`、16 字节正文、immutable；稳定清单和下载页为 no-cache |
| 香港下载入口 | `/downloads/*` 为 302，目标为 `https://download.dsponline.cn` |
| 浏览器 smoke | 香港 1440×900、390×844，上海 1440×900 和下载页均无 page error、request failure 或横向溢出 |

香港切换后的首轮公网 API 验收出现两次 20 秒超时，随后本机健康约 2 ms、公网约 0.23 秒；延长观察 12/12 次均为 200（约 0.23 至 0.46 秒），服务未重启且 journal 无 warning。上海连续 6/6 次健康请求均为 200（约 0.027 至 0.037 秒）。

本轮没有使用真实生产账号执行登录、云读取或云上传，也没有写入玩家存档。未授权边界、CORS、服务测试和备份副本启动兼容已验证；真实账号端到端写入仍是明确的未验证项目。

## 当前指针与回滚

- 香港 Web/API 当前：`web-1.0.31-19040a9d1e45` / `api-1.0.31-19040a9d1e45`
- 上海 Web/API 当前：`web-1.0.31-19040a9d1e45` / `api-1.0.31-19040a9d1e45`
- 上海下载页当前：`download-site-1.0.31-19040a9d1e45`
- 三处代码/下载回滚目标：`1.0.30-c6d896ae6911`
- Web/API 回滚：各节点执行 `sudo dsp-idle-switch-release --rollback-last`，只回滚代码并保留当前数据库
- 下载页回滚：把 `/var/www/dsp-idle-downloads/current` 原子切回受保护 `previous-download-release` 指向的 1.0.30 目录

发布后仅删除明确列出的上传 tar 与失败候选 `/tmp` 副本；release 目录和数据库备份全部保留。最终根盘使用率约为香港 76%、上海 81%。

## 剩余风险

- Windows 安装程序仍为 `NotSigned` 测试包，Windows 可能显示未知发布者或 SmartScreen 提示。
- 香港 2.1 GB 数据库使启动和启动后整理存在短时延迟；本次延长观察稳定，但仍需持续监控首轮健康延迟、RSS、WAL 和磁盘增长。
- 上海根盘处于约 81% 使用率，已进入告警区；不得删除当前版、1.0.30 回滚版、有效数据库备份或原生历史安装包，应按既有归档策略处理后续增长。
- 没有真实账号云写入、实体 Android 设备长时间运行、物理 iPhone 或 Windows 可信签名验收；这些项目不得写成已验证。
- 快速离线与高倍率挂机采用受门禁约束的近似结果，仍存在公开记录的数值偏差和低内存移动设备风险。
