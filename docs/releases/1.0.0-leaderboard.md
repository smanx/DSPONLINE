# `1.0.0` 排行榜资格热修复发布记录

> 发布日期：2026-07-24
> 基线 Git 提交：`01492dea3c513392fb3336daae24c742aac320ce`
> Release ID：`1.0.0-leaderboard-01492dea3c51`
> 构建 ID：`1.0.0+01492dea3c51.leaderboard`
> 应用版本：`1.0.0`
> GameState：v34
> 存档 envelope：v2
> 云 API schema：v7
> SQLite 存储布局：v2

## 发布范围

- 排行榜提交取消邮箱验证门槛。
- 提交仍要求有效登录会话和一份可校验的主云存档；匿名用户只能查看，缺少主云存档时服务端返回明确提示。
- 服务端继续从主云存档重算并约束成绩，隐私账号撤榜和既有赛季规则保持不变。
- 前端只有服务端确认成功后才记录已提交状态，不再产生本地假成功。
- 邮箱验证继续只用于找回密码。邮件发送器未开放时，用户名登录、四槽云存档和排行榜不受影响。
- 本次热修复不改变 `GameState`、存档、云 schema、SQLite layout、活动配置或排行榜已有数据。

发布制品从 `01492dea3c51` 基线、已经在线的客户端下载入口和本次定向补丁组成，并在隔离目录构建；未包含主工作树中的其他开发改动。

## 测试与制品

| 检查 | 结果 |
| --- | --- |
| `npm run build` | 通过，包含 TypeScript 严格检查 |
| `npm test` | 42/42 文件，431/431 通过，1 项可选基准跳过 |
| `npm run test:server` | 31/31 通过 |
| `npm run test:ops` | 5/5 通过 |
| `npm run test:native` | 2/2 通过 |
| 排行榜 focused Playwright | 2/2 通过 |
| `npm run test:e2e` | 131/131 通过，约 5.4 分钟 |
| 远端 `npm ci --omit=dev` | 两地各 76 个包，0 个已知漏洞 |
| 远端服务端测试 | 香港 31/31，上海 31/31 |
| Release manifest | 115 个文件验证通过 |

制品摘要：

- 聚合 SHA-256：`b06147319f969fade7e1b7587a32c466ced14453388ec6f02b2d8e09be3b0bf9`
- Web 归档 SHA-256：`3309da9b1148a72b7b92b76131ed622bd3df4ca07168333a82bc146e50598705`
- API 归档 SHA-256：`8a702e478f83618d74f23e76516604761b12c026ce03b83779c37e8733ae6464`
- 两地激活后的 `index.html`、排行榜 chunk 和 `server/index.mjs` 均与本地制品逐文件哈希一致。

完整 E2E 仍记录既有的非阻断 Vite `ResizeObserver loop completed with undelivered notifications` 卸载提示，131 项断言与最终退出码均成功。

## 数据保护

香港：

- 发布前备份：`manual-leaderboard-pre-20260724T154915Z.sqlite`，274,939,904 字节，SHA-256 `690a1b4521d5bc487eec5e9fc7b172ea1b474e21d423f71fd6a857b2c8541f1d`。
- 发布后备份：`manual-leaderboard-post-20260724T160106Z.sqlite`，SHA-256 `0efe02ccc4336682fa0376aa4ba2376853a9a674c643e248fac1646c83e01c11`。
- 发布前后账号 `103→104`、会话 `161→163`、主云存档 `81→81`、有效历史修订 `738→744`、排行榜提交 `0→0`；变化来自发布窗口内正常生产流量，没有记录减少。

上海：

- 发布前备份：`manual-leaderboard-pre-20260724T155050Z.sqlite`，77,824 字节，SHA-256 `3841ec9703d41b84cec7c962f5103f57cd8ba43659188ff3427df5c2c957f291`。
- 发布后备份：`manual-leaderboard-post-20260724T160106Z.sqlite`，SHA-256 `d39a81314b7d1e7e305c2e3cbd6ae26d3035284fc2dfa58c25b97073a1f8967b`。
- 账号、会话、云存档、历史修订和排行榜记录均保持为 0；匿名玩家仅发生正常增长。

四份快照均由 SQLite Backup API 创建并通过 `quick_check`。发布没有恢复、替换或初始化数据库，没有使用生产账号执行写测试，也没有上传测试存档。

## 公网验收

- 香港 `https://dsponline.cn`、上海 `http://shanghai-node.example.invalid` 和两地 `/api/health` 均返回 200；香港 `www` 保持 301。
- 两地 Web/API 都指向 `1.0.0-leaderboard-01492dea3c51`，`NRestarts=0`。
- schema v7、SQLite layout v2 和邮件 provider `disabled` 保持不变。
- 联合空间站活动保持 `active`，revision 仍为 `e227b03a6fbd4d148b3f07ad`。
- hashed JS 使用 gzip 和 immutable 缓存，HTML 保持 no-cache。
- 两地 systemd journal 在发布窗口内无 warning，最近 500 条 DSP 访问日志无 5xx。
- 约一分钟观察窗内，香港 6/6、上海 6/6 健康请求成功；香港服务内存稳定在约 425～431 MiB，上海约 18～19 MiB。

两地回滚目标均为 Web `1.0.0-downloads-01492dea3c51` 与 API `1.0.0-01492dea3c51`。回滚只切换代码，不恢复数据库。
