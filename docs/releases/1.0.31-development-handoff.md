# 1.0.31 开发交接

Task ID / title: 2026-08-05 玩家反馈批次（11 项）
Priority: P0/P1/P2，详见 `docs/feedback/DEVELOPMENT_BATCH_HANDOFF_2026-08-05.md`
Release target: 1.0.31；香港 Web/API、上海 Web/API、上海下载页；Android/Windows 包随下载清单更新
Compatibility: GameState v46、存档 envelope v2、云 schema v7、SQLite layout v2 均保持不变；新增 UI 偏好仅保存于设备 localStorage

## 隔离来源

本候选从已发布 1.0.30 clean 基线 `c6d896ae691147c671ab784eccaf704b23a6609b` 建立独立 staging，再叠加本批源码和测试。共享主工作区仍保留所有其他 Agent 未提交改动，不能从共享工作区直接发布。

## 代码与专项验证

当前 staging 已覆盖离线报告布局、生产历史窗口和有界曲线、取消科技续队列、快速离线游标归一化与安全回退、施工库存删除、物品悬浮开关、移动统计滚动、重整精炼文案、锁定配方拓扑保护、零 tick 线路计数和高倍率纯挂机近似治理。

已实际运行：

- `npm ci` / `npm --prefix server ci`：隔离依赖安装通过。
- `npm run licenses:check`：128 个运行时包许可证清单一致。
- `npm run typecheck`：通过。
- `npm test -- --run`：87 个文件通过、5 个跳过；778 项通过、16 项跳过，0 失败。
- 目标 Vitest：8 个文件通过；336 项通过、1 项跳过。
- 反馈批次 Playwright：9/9 通过。
- 真实存档 Playwright：2/2 通过；只读夹具未被写回。
- `npm run test:e2e`：246 项通过、2 项显式可选真实夹具/故障注入跳过，0 失败。
- `npm run test:server`：49/49 通过。
- `npm run test:ops`：6/6 通过。
- `npm run test:native`：8/8 通过。
- `npm run build`：通过，Vite 转换 1,878 个模块。
- `git diff --check`：通过。

真实存档验收记录：

- 快速离线 10 分钟、1 小时、7 天、30 天全部返回 `approximate`，单档约 0.40～0.52 秒；序列化读回、关键字段和源状态不变检查通过。
- 8x、12x、16x 各执行 1,800 秒虚拟墙钟、225 个连续切片，实际推进倍率分别为 8/12/16，近似覆盖率 100%，0 次精确回退；P95 分别约 84.94/72.23/70.84 ms，最大切片约 114.92/83.23/74.85 ms。
- 三档首切片确定性和源状态不变检查通过，峰值 RSS 约 527 MB；浏览器 Worker 停止约 256 ms，终止后无迟到消息。
- 只读夹具测试前后文件 SHA-256 一致。快速路径按既有 1.0.30 策略保留最高 100% 的关键尾验误差上限，不能宣称全状态精确或任意设备 30 秒硬保证。

## 发布门禁

`codeReady: true`；`releaseReady: false`。仍需在本 clean 提交上完成：

1. 创建 clean 候选提交，并从该提交生成 build ID、Web/API/下载页/Android/Windows 不可变制品和聚合 manifest。
2. 验证 Android 长期签名证书连续性；Windows 继续明确记录 `NotSigned`，不得创建新证书。
3. 完成两地 SQLite Backup API 快照、未激活目录依赖启动、原子切换、公网健康和下载 Range/缓存验收。

在以上门禁完成并记录前，不连接 VPS、不修改生产 symlink、不更新公开下载页。
