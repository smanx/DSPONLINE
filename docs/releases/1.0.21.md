# 1.0.21 / v46 紧急稳定性与 P3-P6

发布日期：2026-08-02  
发布提交：`830c9382233e`（核心实现提交：`bee7a54`；P0-P2 基线：`f71f9b0`）  
构建 ID：`1.0.21+830c9382233e`  
GameState：v46；存档 envelope：v2；云 schema：v7；SQLite layout：v2

## 本次内容

- P0-P2：云存档大正文边界和错误分类、终局极限模式、蓝图精简/详细布局、巨构蓝图启用意图与相关回归。
- P3：保存任务复用最近一次已验证的不可变快照；revision、主键变化或写入失败时自动回到完整保存，不删除自动保存和急救镜像。
- P4：新增带 `baseRevision/nextRevision` 的模拟增量协议。默认继续使用完整状态；只有本地开发键 `dsp-idle-network.experimental-simulation-delta.v1=true` 才启用，命令边界/首次加载/版本不匹配自动回退完整状态。
- P5：新增批量传送带绘制层。仅在终局极限模式、当前星球线路至少 600 条且 `dsp-idle-network.experimental-canvas-batch.v1=true` 时启用，建筑、命中路径、选中线路和连接预览仍由原路径负责。
- P6：新增多 Worker 规划安全门槛。工作量不足、Worker 少于 2 个或实测提升不超过 15% 时始终使用单权威 Worker，最多规划 4 个 Worker，不改变正式确定性模拟。
- 运维：香港大库启动时原 8 秒健康探针会误重启服务，探针模板和香港 live unit 已改为 60 秒；timer 成功运行一次后服务保持 active、`NRestarts=0`。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test` | 74 个文件通过、1 个跳过；651 项通过、9 项跳过 |
| `npm run test:server` | 46/46 通过 |
| `npm run test:ops` | 6/6 通过 |
| `npm run test:native` | 7/7 通过 |
| `npm run build` | 通过，1,866 个模块 |
| `npm run test:e2e -- --workers=1` | 198/198 通过，约 9.8 分钟 |
| 新 API 远端测试 | 香港 46/46；上海 46/46 |
| 隔离备份副本启动 | 两地 `/api/health` 均返回 schema v7/layout v2 |

完整 Playwright 的本地 Vite 代理日志中仍有 `127.0.0.1:4320` 未启动提示；相关夹具均通过，正式公网 API 已另行验证。

## 真实性能回归

使用 `dsp-idle-save-2026-08-01 (3).json` 的只读副本：913 个实体、1,375 条传送带、17 颗星球。推进 1,800 秒耗时约 40.69 秒；单次推进与 60 次分段推进的实体、线路、库存和生产结果一致，权威状态哈希为 `0a96182e`，73 类库存均为非负安全整数。完整 `hashGameState` 若包含生产历史会变化，这是 UI 运行时诊断字段；正式持久化投影会清除该字段。

只读多 Worker 探针中位墙钟：1 Worker 36.01 ms、2 Worker 18.75 ms、4 Worker 11.61 ms、8 Worker 12.69 ms。4 Worker 相对串行协调器约 2.28 倍，8 Worker 约 2.09 倍；这是星球分区探针，不是完整确定性模拟，因此 P6 默认关闭。当前星球 369 个线路段的 Canvas 打包中位 0.034 ms、P95 0.08 ms。

## 制品与部署

发布清单包含 146 个文件，聚合 SHA-256：`541d9eeca5d7072ab45b866a2c3a91b3f00cd7c2bdc9c896001c16fb2617269a`。

| 制品 | SHA-256 |
| --- | --- |
| Web tar | `6fb05f172c8b336da2245c654e3bf788b6000c2ee45114336d038649fffbcad6` |
| API tar（含邮件模板） | `7c7141eadf891fd6a89fcf58b5f84e356c217b395f553dca2d78df084a4d5fdd` |
| 上海下载页 tar | `0e97658cb8040c02acf5d4de1250b0dc3eb855cea2386bb6943a72949314d5f7` |
| Windows x64 安装包 | `886d28ab0a81f75380e0b9f9fafd41e15be0696c60e17e8a4972f1fae744e743`，112,178,774 字节 |
| Android 1.0.21/1000021 | `441dadd6de08882387cf98660b9263106fa69718dcbe45fa325d1836edceeabe`，4,478,143 字节 |

香港和上海 Web/API 当前目录均为 `/releases/1.0.21-830c938`；上海下载页当前目录为同名目录，上一版 `1.0.21-f71f9b0` 保留为回滚目标。公网 `version.json` 为 `1.0.21+830c9382233e`，Android `stable.json`、Windows `latest.yml`、EXE Range `206` 和 APK/EXE 完整下载哈希均通过。

Android 本轮没有重新签名：长期 keystore 环境变量仍未配置，为保护旧版覆盖升级链继续使用已验证同证书包。该 APK 内部 build ID 仍为 `1.0.21+f71f9b0a855c`，因此 P3-P6 先在 Web 和 Windows 生效；恢复长期密钥后应单独构建、覆盖升级验证并更新 Android 清单，不能用新证书替代。

发布前 SQLite Backup API 快照：香港 `1,283,846,144` 字节、SHA-256 `9926623ca1ece52ab79884080ae87e2a56b2d3e4605b453e2d4ae30934272a72`；上海 `126,976` 字节、SHA-256 `d1240fa4d758ddf8353bb8a555c51422dfd15308c574e6b16e71cafd68cbd045`。两份均通过 `quick_check`，未恢复或替换生产数据库。

香港大库发布期间磁盘最低约 2.0 GiB 可用，服务启动曾被旧探针误判一次；停用补跑、完成启动并将探针改为 60 秒后，公网 health 连续通过，服务 `active`、`NRestarts=0`。后续应继续清理已验证旧备份/发布归档并维持至少数 GiB 安全余量，不得删除当前库或唯一恢复点。

## 已知取舍

- 极限模式和 P3-P5 的实验路径会减少线路动画、普通读数刷新或非关键 DOM 更新；模拟、产量、存档哈希、物流和库存不改变。
- 增量协议、批量线路层和多 Worker 仍是开发开关/安全门槛路径，默认关闭；不能把探针加速直接等同完整模拟加速。
- Windows 安装包未配置 Authenticode，下载页继续标注未知发布者风险。
- Android 因签名连续性保护暂未包含 `830c938` 的文本校正和实验开关变更，不能宣称 Android 已获得本轮 P3-P6；后续必须在长期签名环境恢复后单独发布。
