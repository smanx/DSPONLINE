# 安全政策

## 支持范围

当前仅维护最新的 `1.x` 分支。旧版本可能仍能读取存档，但不会持续获得安全修复。

## 报告漏洞

请使用 GitHub 仓库的 **Private vulnerability reporting** 提交安全问题，不要创建公开 Issue。仓库公开后，维护者应在 GitHub 的 `Settings > Security > Code security and analysis` 中启用该功能。

报告应包含受影响版本、复现步骤、影响范围和建议缓解方式。不要附带真实密码、访问令牌、私钥、玩家邮箱、生产存档或数据库副本；必要证据应使用本地临时环境和合成数据。

以下问题尤其应私下报告：

- 账号接管、会话绕过、云存档越权或排行榜验证绕过；
- 管理员接口、备份、恢复、更新签名或发布渠道问题；
- 可读取其他玩家数据、执行任意代码或持久化脚本的输入；
- 泄露仍然有效的生产凭据或签名材料。

维护者会在能够确认范围后协调修复和公开时间，但本项目不承诺固定响应 SLA 或漏洞奖励。

## 安全边界

社区构建默认不连接官方 API 或更新源。只有官方发布流水线会显式注入正式地址，官方原生安装包还必须通过平台签名门禁。自建实例的密钥、数据保护、隐私政策、备份和更新安全由该实例运营者负责。
